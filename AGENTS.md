# AGENTS.md

## 项目概述

Surgery RAG Agent：外科领域垂直 RAG AI Agent，包含医生/患者问答、知识库管理、AI 操作者纵向病例、预测报告及 PDF 归档。

- 前端：Vue 3 + TypeScript + Element Plus + Pinia。
- 后端：Python FastAPI，LangChain LCEL、RunnableWithMessageHistory、DeepSeek LLM。
- 检索：langchain-postgres PGVector，BGE-M3（1024 维），向量检索 + PostgreSQL 全文检索 + RRF 融合。
- 报告：结构化报告、固定输入和版本快照、持久后台任务、历史读取及 PDF 原件归档。

## 先理解，再修改

- 如果信息不够完善或在多种实施方案间存在歧义，请先向我提问澄清，不要自行猜测或假设。
- 提问前先检查相关代码、测试和文档；仓库能够明确回答的事实先自行查证，仍有歧义时说明具体问题与方案差异。
- 多步骤任务开始前简述目标、修改范围和验收方式；明确的小改动可直接实施并验证。
- 区分请求类型：解释、审查和诊断先给出证据；用户要求实施时，在明确范围内完成修改和验证。
- 当前实现与文档不一致时指出差异，不依据历史计划自行扩展或重写功能。

## 简单实现，精准修改

- 每项改动都应能对应本次需求。优先复用现有模块、组件、数据契约及错误处理方式。
- 不添加未经要求的功能、通用框架、抽象层、依赖或配置项。
- 保持现有代码风格；不顺手修改无关格式、注释、文件名和模块结构。
- 清理本次改动造成的未使用代码；发现原有无关问题时记录说明，不擅自删除。
- 保留用户未提交的改动，不覆盖、回退或混入不相关工作。

## 按任务读取文档

- 项目结构与功能入口：README.md。
- 运行时、依赖恢复、测试与默认跳过项：docs/DEVELOPMENT.md。
- 所有 UI 修改：先完整读取 docs/DESIGN_SPEC.md。
- 报告任务、历史记录、PDF 归档或 worker：docs/OPERATOR_REPORT_OPERATIONS.md。
- 部署任务：docs/DEPLOY.md；阿里云部署另读 docs/ALIYUN_DEPLOYMENT_RUNBOOK.md。
- docs/superpowers/ 中只读取与当前任务相关的设计和计划，并确认状态；历史方案不自动成为本次任务。
- docs/BASELINE.md 是带日期的验证记录，不代表当前环境仍然通过。

## 前端 UI 与接口

- 任何 UI 修改前，必须先读取 docs/DESIGN_SPEC.md 并严格遵循其中全部规范，除非用户明确要求更改风格。
- 维持暖杏蓝色彩体系及规范中的排版、间距、圆角、阴影、动效、组件变体与布局尺寸；优先使用第 10 节定义的 CSS 变量。
- 保持医生/患者体验差异，并遵循管理员、AI 操作者的既有页面职责和权限逻辑。
- 复用现有组件、API 封装和 Pinia 状态管理；不要在页面中重复实现已有业务逻辑。
- 接口或 SSE 事件变化时，同步检查后端 schema、前端类型、调用方及相关测试。
- 报告文档类型以 backend/app/schemas/report_document.py 为来源，通过 scripts/generate_report_document_types.py 更新前端生成类型，不单独手改生成文件。
- 保留 Markdown/HTML 内容净化；页面隐藏按钮不能替代后端授权校验。

## RAG、病例与报告约束

- 修改检索时保留文档访问范围、科室及当前版本等既有过滤；不得为提高召回率绕过访问控制。
- 引用、病例片段和参考标准必须可追溯；缺少证据时明确表示缺失，不编造病例、指南、数值、单位或来源。
- 保持真实、合成、演示和测试数据的来源标识；测试数据与演示指标不得冒充真实临床证据或正式验证结果。
- 单位、时间顺序、缺失值及输入完整性按照已有契约处理；不为让报告生成成功而静默填值、改变阈值或降低验证条件。
- 模型、标准、数据集和报告快照沿用既有版本管理与审计机制；修改这些机制前明确影响范围。
- 历史报告读取保存的输入、证据、模型版本及报告内容；不得调用当前模型重算来补齐或替换历史结果。
- PDF 下载只交付已发布的归档原件；缺失或损坏按运维文档恢复，不即时重渲染替代原件。
- 保持持久任务、幂等、取消、超时与租约语义；断线仅恢复观察，不自动重新发起生成。
- 涉及病例、报告、来源或下载时，验证用户归属、角色及疾病权限等适用限制；保留既有医疗内容过滤和审计。

## 运行环境与数据操作

- 使用项目既有 backend/.venv 与 docs/DEVELOPMENT.md 指定的运行时：Python 3.11.4、Node.js 22.15.0、npm 10.9.2。环境升级作为明确任务处理。
- 前端只使用 npm 和 package-lock.json，不混用包管理器。
- 业务表变更通过 backend/alembic 迁移；database/schema.sql 仅是参考，LangChain 内部向量表由 langchain-postgres 管理。
- 编写迁移和在已有业务数据库执行迁移是不同操作；执行前确认目标环境与任务授权。常规验证遵循开发文档的数据库只读边界。
- 不在日志、测试输出、提交或回复中暴露密钥、连接串和可识别患者信息；配置说明使用变量名和占位值。
- 默认本地验证不调用外部 LLM、不下载模型、不重新索引、不训练或发布模型、不写业务数据库。相关专项任务明确要求时，按其范围和文档执行。
- 集成/E2E 测试使用明确的隔离测试数据库；缺少配置时报告未执行，不能回退到业务数据库。
- 数据清理、生产部署、推送和模型/标准发布必须在用户授权范围内；不要因常规代码修改自动执行这些操作。

## 验证与交付

- Bug 修复先复现；可自动化时增加能揭示原问题的回归测试。新增业务行为验证成功、失败和适用的权限路径。
- 断言检查实际行为，不能仅检查函数名、文本存在或只返回成功状态；涉及同分排序等情况应断言实际顺序。
- 根据改动风险选择检查，先运行相关测试；跨模块或关键流程修改再扩大回归。纯文档、注释或简单样式改动不强制新增单元测试。
- 前端行为或接口变化运行相关 Vitest/契约测试及构建；UI 变更还检查实际页面、交互和适用的加载/空/失败状态。
- 后端修改运行相关 pytest；数据库、worker、归档或权限行为需要适用的集成验收，不能用 mock 通过代替真实链路验证。
- 环境不足导致无法验证时，报告缺少条件及影响；不修改测试标准、跳过断言或降低安全检查来制造通过结果。
- 完成时说明修改摘要、实际执行的检查、结果及未验证项。保留失败和跳过的区别，不将历史记录当作本次验证结果。

常用命令（按改动范围选择，运行前核对开发文档）：

- 后端：在 backend 目录使用 .\.venv\Scripts\python.exe -m pytest tests ../scripts/tests --ignore=tests/integration --ignore=tests/e2e。
- 前端：在 frontend 目录运行 npm run test:unit、npm run test:contracts、npm run build。
- 完整开发基线：在项目根目录运行 powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/verify_baseline.ps1；此命令会更新 docs/BASELINE.md，适用于需要刷新基线的任务。
