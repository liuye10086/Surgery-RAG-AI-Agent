# 合成数值报告C包验收记录

日期：2026-09-14。状态：worker执行、原子发布及固定历史读取完成；页面与PDF进入下一步D包。

## 已完成范围

- 新增严格`synthetic_numeric_report_document.v1`和SyntheticPublication，原Publication与旧文档类型保持；独立`v3`指纹绑定输入、上下文、数值结果、正文、工程证据说明和结构化文档。
- worker按上下文分派，父进程按Publication版本解析。子进程先核验固定输入，再匹配接单时算法身份并执行末次值保持；父进程重建并比对发布内容，最后在有效租约及同一事务中更新任务、报告和终态审计。
- 数值文档展示历史实测、6／12月结果、目标日期、单位和不可用原因，明确“合成数据 · 末次值保持基线 · 尚无临床有效性结论”。没有临床标准／参考病例检索、LLM、风险概率或治疗建议。
- 0028迁移增加v3和工程专用`not_requested`状态，旧报告不能借此跳过临床证据约束；有已发布v3事实时拒绝downgrade。仅在任务私有PostgreSQL实例执行。
- 历史列表、详情及失败／取消输入审计识别新类型；绑定所有者、病种、来源病例、batch、创建时间与保存上下文。未知版本、篡改或降级旧格式均拒绝，隐藏不可信内容。原病例删除后仍从保存内容读取。
- 历史校验不导入数值执行算法模块、不读取当前算法源码、不重新预测。独立新进程阻断相关导入和源码读取后仍能验证保存报告。
- 前端数值文档DTO与联合类型由既有生成脚本产出；本轮不改页面。PDF准备入口明确拒绝v3，等待D包专用模板，不能误用旧模板。

## 本轮最终验证

| 检查 | 实际结果 |
| --- | --- |
| 相关后端单元／真实子进程监督测试，15个文件 | 250 passed，24.86秒，4项弃用警告 |
| 新执行27项、B包24项及旧任务／历史／审计／API集成，6个文件 | 71 passed，62.12秒，4项弃用警告 |
| 前端单元 | 20个文件、115 passed，9.21秒 |
| 前端契约 | 30 passed |
| 前端构建 | 成功；依赖注释、混合导入及大块体积提示保留 |
| 独立最终只读审查 | 规范符合性通过、代码质量通过，未发现剩余重要问题；审查者未运行测试 |

数据库集成通过FastAPI TestClient调用真实路由、服务及PostgreSQL，`run_worker_once`启动真实spawn子进程，覆盖两病种四任务从接单到发布、读取及跨用户拒绝。取消、租约过期、执行期限、事务flush后失败均验证未发布或保持唯一终态；错batch、报告ID、task、日期、单位、来源和数值被拒绝。父进程强制终止及子进程回收在共享监督器真实进程测试中验证。不可用输入经过真实父子进程后仍保留空值和原因；冻结的240主体包全部为可用输入，因此该不可用场景使用明确测试fixture，没有篡改来源包。

先复现B包无消费者／新上下文历史无法解析，再实现C包。复审阶段补出并修复历史行身份、上下文关联和降级旧完整性分支的失败用例。一次中断使末轮测试结果无法确认，恢复后串行重跑以上最终后端及前端单元验收并保存日志；未将中断算作通过。最终验收无失败、无跳过。

## 保全与限制

本轮前913个文件的保全核对没有缺失，变化仅限C包源码、测试、生成类型及进度文档；167个既有合成输出文件保持摘要，A包源码及模型制品未改。阶段二第5节36项逐字保留：13项满足、15项缺条件、8项未执行。

默认`SYNTHETIC_REPORTS_ENABLED=False`不变；关闭新接单不取消已受理任务。未写已有业务数据库，未调用外部LLM、训练、发布模型、提交、推送或部署；私有测试实例已停止。独立HTTP服务、worker CLI、浏览器和PDF联合验收留到D／E包，本轮结果不能当作产品全链路或生产验收。

下一步D包：操作者页面、历史展示、专用PDF模板及归档原件验收。其后E包：完整隔离链路验收。真实路线仍需A2／A3事实与组样、A4评价合同、A5冻结，再进行正式独立比较、接入及获准发布；合成工程通过不代表临床泛化有效。
