# 本机前后端与两病种 PDF 实测

日期：2026-09-15。

按用户当前批准的“先用合成数据跑完最终流程”范围，阶段二工程工作已完成，剩余开发步骤为 0。真实资料审核、真实数据训练与独立评价属于资料到位后的工作；原正式冻结清单保留，不将工程验收替代真实临床验证。

## 本次实际执行

- 用户指定本机已启动 PostgreSQL 中的 `surgery_rag_test`。只读预检确认该库为 0026，用户、病例、报告、文档和分块均为空；执行 Alembic 升级至 0030。
- 创建一个专用 AI 操作者账号，导入 AD、脂肪肝各一个合成病例，以及 16 条带后端来源标识的输入参考记录。
- 使用 2026-09-15 `switch-v2` 合成包及 `model-v2/bundle.json`；未重新训练或发布模型。
- 启动完整应用 `app.main:app`（8000）、Vite 前端（5173）、常驻报告 worker 和 PDF worker。SQL 与向量连接均显式指向用户指定的测试库。
- 从真实前端登录、选择病例、提交报告，等待真实检索与 DeepSeek 说明生成，进入历史报告、准备 PDF 并下载归档原件。两次报告请求均成功，未重试 LLM。

| 病种 | 报告 ID | 参考记录 | PDF 页数 | 状态 |
| --- | --- | --- | --- | --- |
| AD | 1 | 7 | 3 | 报告 completed，归档 ready |
| 脂肪肝 | 2 | 7 | 3 | 报告 completed，归档 ready |

两个报告均为 v5，向量与全文检索状态均为 complete，说明响应模型为 `deepseek-flash`。下载字节与归档原件一致，PDF 日期、数值、单位与保存的预测一致，前端无 pageerror。六页逐页检查，文字表格清晰；部分参考记录跨页，末页留白较多，留待用户评估。

测试脚本曾因登录/注册输入框重复定位，以及项目运行时缺少 PDF 检查库而中止。已修正测试定位并使用 Codex 自带的 pypdf、Poppler 检查；已完成报告从历史继续读取，没有重复生成。未修改产品代码、模板或项目依赖。

## 保存位置与运行状态

结果：`outputs/local-device-preview/2026-09-15/result.json`；数据库和归档核验：同目录 `final-check.json`；PDF 文本核验：`pdf-verification.json`；PDF：`ad.pdf`、`fatty_liver.pdf`。归档目录 `archive` 和已固定资源目录 `renderer` 需保留。

服务保持运行，页面为 `http://127.0.0.1:5173`。本机登录和启停说明保存在 `.tmp/local-device-preview-20260915/使用说明.md`，账号凭据不写入版本文档。配置仅覆盖本次进程，未修改 `backend/.env`。常规启动仍读取原配置，需要继续使用本次预览启动脚本才能连接测试库。

完成后的只读检查确认原 `surgery_rag` 仍为 0026，用户 3、病例 8、报告 13、文档 9、分块 1063，与预检一致。本次未对原库执行迁移、导入或清理。未运行无关全量测试或刷新 BASELINE。

下一步：用户评估两份 PDF 和页面，按反馈确定需要调整的内容与排版。当前合成工程路线剩 0 步；真实资料到位后执行同契约导入审核、适用训练/独立评价、版本绑定。

## 阶段二提交前核验（2026-09-15）

用户确认以“预测线路修改或重构第二阶段”提交并推送当前阶段二变更。总领文档更新至 v0.77，阶段二计划更新至 v0.73，首页统一标记当前合成工程范围完成，并将原开放条目明确列为真实资料到位后的核验依据。两份文档所有编号章节及原验收清单内容保持不变，首页链接校验通过。

本次重新执行：后端数值报告、合成报告、完整性和验收脚本等相关测试 180 项通过；前端单 worker 单元测试 22 个文件、151 项通过；契约检查 30 项通过；TypeScript 检查与生产构建通过。首次 npm 调用未传入串行参数，出现 3 项超时和 1 项后续断言失败，随后直接使用 `node node_modules/vitest/vitest.mjs run --maxWorkers=1 --minWorkers=1` 全量复跑通过，未修改测试断言或超时阈值。构建仍有依赖注释、混合导入和包体大小提示，后端仍有 Pydantic 旧配置写法警告。

清理一个数值报告 schema 文件末尾的多余空行后，暂存差异格式检查通过。暂存区不包含本机配置、凭据、数据库、模型制品或生成 PDF，也未匹配到当前配置中的密钥。此轮未再次调用外部 LLM、执行数据库写入验收或运行完整开发基线；两病种真机链路结果见上文。
