# 合成数值报告B包验收记录

日期：2026-09-14。状态：来源绑定与接单完成；数值执行、发布和完整历史读取尚待C包。

## 已完成范围

- 新增服务端合成来源schema、固定目录包校验、病例／访视投影摘要及原子种子导入。客户端不能提交来源绑定；工程病例输入通过普通保存和访视增删改入口均不可编辑。包加载不读取未来结局。
- 迁移0027增加可空来源列和数据库约束，不重标记旧病例；有来源数据时拒绝破坏性downgrade。仅在本轮新建的本机独立PostgreSQL测试实例执行迁移。
- 显式`report_kind=synthetic_numeric`进入独立来源、角色、归属、疾病、输入及算法身份校验，保存固定快照／上下文；缺省请求的旧哈希保持逐字兼容。工程病例不能走旧报告入口，普通病例不能走合成入口。
- 复用现有幂等、容量、持久任务、取消和排队超时；关闭开关后允许已接单同键重放。旧worker暂不认领新数值上下文。前端API封装支持显式类型，现有调用仍保持旧请求形状。
- 最终疾病锁读取刷新ORM缓存，避免首次读取后停用疾病仍被受理；种子和接单均覆盖该窗口。隔离连接守卫拒绝非空`PGHOSTADDR`／`PGSERVICE`及URL查询参数，避免连接目标被覆盖。

## 本轮实际验证

| 检查 | 结果 |
| --- | --- |
| 来源、接单、A包及病例／报告相关后端单元测试，16个文件 | 301 passed，8.58秒，2项弃用警告 |
| 新接单24项及既有接单、报告API、病例API、持久任务集成，5个文件 | 54 passed，35.70秒，4项弃用警告 |
| 前端全部Vitest | 20个文件、115 passed |
| 前端契约 | 30 passed |
| 前端构建 | 成功；依赖注释、混合导入和大块体积提示保留 |
| 独立只读复审 | 疾病缓存窗口及libpq环境覆盖问题修复后未发现新增重要问题；复审本身未执行测试 |

集成实际使用独立PostgreSQL 18／pgvector，迁移到0027，覆盖两病种来源导入、事务失败回滚、重复导入、DB约束、同键并发、跨种类冲突、权限／归属、来源篡改、只读输入、取消、排队超时、固定快照及旧worker误领防护。旧报告执行回归通过，不代表新数值worker已实现。

调试中先复现来源／请求负例及疾病并发问题，再修复验证。疾病错误映射曾返回通用失败，已改为`disease_disabled`／409；旧单元测试查询替身补齐刷新链式调用。最终上述验收无失败、无跳过；早期失败没有记成最终通过。

本轮前保全记录含788个既有文件，验收时检查变更仅限B包明确涉及的源码、测试和进度文档，没有缺失文件；原合成输出、A包代码及模型制品保持原摘要。阶段二第5节36项逐字保留：13项满足、15项缺条件、8项未执行。

## 限制与下一步

`SYNTHETIC_REPORTS_ENABLED`默认关闭。B包仅完成接单：新任务停留排队，可取消或超时；完整历史读取尚不能解析新上下文，因此不开放产品入口。未运行新数值worker、浏览器或PDF端到端验收，未写现有业务数据库，未调用外部LLM、训练、发布模型、提交、推送或部署。测试实例验证后停止。

下一步C包：独立数值worker、结构化报告原子发布及固定历史读取。剩余D包：页面／PDF；E包：完整隔离链路验收。真实路线仍需A2／A3事实与组样条件、A4评价合同、A5冻结，再进入正式比较、接入及获准发布。合成规模和本轮工程通过不能替代真实临床泛化证据。
