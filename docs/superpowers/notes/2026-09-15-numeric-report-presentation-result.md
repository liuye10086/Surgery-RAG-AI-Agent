# 数值报告展示调整结果

日期：2026-09-15。用户批准的三项展示调整均已完成；本轮未提交或推送。

## 修改

- 训练数值报告的模型及说明模型信息集中在正文末尾；PDF 去掉标题之前重复的算法版本条。
- 页面与新 PDF 的预测值、比较基线统一显示两位小数，提示展示四舍五入。历史实测精度、后台浮点值、canonical 正文与完整性指纹不变；缺失值仍为破折号。
- 在现有 `report_pdf.html` 内为数值报告增加参考附录局部样式。每条参考按完整记录块控制分页，保留全部来源、顺序和正文；缩减版本区标题及参考区间距，避免版本区单独占页。

打印改动在保存正文通过验证后应用，继续经过 Bleach 清洗。前端仍使用既有组件及设计变量，不引入依赖，不改变预测模型或数据契约。

## 实际验收

本机 PostgreSQL `surgery_rag_test`，完整 `app.main:app`、前端、常驻报告与 PDF worker。真实浏览器登录、选择病例、生成报告、查看历史并下载 PDF；模型使用原 `model-v2`，未重训。共四次新报告生成和真实说明调用，均成功：中间版报告 3、4 保留，最终版为 5、6。AD 中间版的版本区单独占第 3 页，先用独立本地排版预览确认微调效果，再生成最终版；没有覆盖既有归档。

| 最终报告 | ID | 页数 | 引用数 | PDF SHA-256 |
| --- | --- | --- | --- | --- |
| AD | 5 | 2 | 7 | `3de1abd04eb5646f4773497bd81e89d747f794c0800d03e98c48634e9803a043` |
| 脂肪肝 | 6 | 2 | 7 | `d8c8aad9e537f8cd8d441fe3168acf2e2b647f576740a808558dfa8d3716e241` |

最终四页逐页检查，表格清晰、参考块未跨页拆开、版本信息在末尾；没有为了固定页数删减内容。不同病例长度仍可能产生不同页数。页面实测无 pageerror，结果和基线显示与 PDF 一致。

报告 1—6 均再次通过 API 读取核验：完整性为 valid，输入、正文、预测、结构化文档及指纹与各自保存结果一致；六份下载字节与原记录 SHA-256 一致。未对原 `surgery_rag` 库执行迁移、导入或清理。

## 检查与限制

- 新回归先失败，实施后后端相关检查共 46 项通过；前端相关 Vitest 29 项通过，契约检查 30 项通过，生产构建和最终 TypeScript 检查通过。
- 初次新增测试的 `.at()` 不受项目 TypeScript 目标支持，改为普通数组索引后通过；打印分组测试改为断言实际标题、正文与顺序，避免把 Markdown 自动换行当成正文差异。
- 独立只读代码审查未发现需修复的回归；最后补充的间距调整由实际 PDF 验收确认。
- 保留既有 Pydantic、构建包体与混合导入等提示；renderer 构建过程中出现既有 Playwright 关闭任务提示，但退出成功，manifest 校验及实际归档均成功。
- 未运行完整开发基线，不把本次展示调整或合成报告解释为真实临床有效性验证。

结果目录：`outputs/numeric-presentation/2026-09-15/final-reports`，包括两份 PDF、保存响应、浏览器结果、页面截图、逐页 PNG、文本与归档核验结果。最终 renderer 位于同级 `renderer/176dcb0e47cc31e63e3f78a57b83e6508bc02b1eeadff924b73ec6215bc16cf4/manifest.json`。

原归档根目录继续使用 `outputs/local-device-preview/2026-09-15/archive`；本机预览脚本通过独立 renderer 路径配置使用新版资源，未修改 `backend/.env`。服务保持运行，历史列表中查看报告 5、6 即为最终版。已存在的 `docker-compose.test.yml` 删除记录未处理。

下一步由用户评估最终两份 PDF。本次三项展示调整剩余 0 项；阶段二合成工程范围仍已完成。真实资料审核、训练评价与版本绑定待资料到位后开展。
