# 统一数值报告第2步：完整能力验收记录

日期：2026-09-14。对应[实施计划](../plans/2026-09-14-numeric-full-capability.md)与[统一方向](../specs/2026-09-14-unified-prediction-report-direction.md)。

## 结论与范围

已用合成数据实际跑通重构后的训练模型、BGE/PGVector/全文/RRF检索、DeepSeek说明、持久报告、历史读取及同一模板PDF原件归档。前端使用同一个数值报告组件，来源属性保留在后端；新请求不按真实／合成来源选择不同产品模板。

新增固定JSON模型包、上下文v2、预测v2、文档v2、发布指纹v5和迁移0030。接单固定模型参数及实现身份、参考候选、检索配置、提示全文/SHA和说明模型；worker使用保存参数，配置漂移拒绝执行。历史只校验保存事实，不调用当前模型、检索、LLM或当前过滤配置。旧v1—v4报告和归档机制保持兼容。

服务端 `NUMERIC_MODEL_BUNDLE` 配置指向固定 `bundle.json` 后使用完整路径；为空时保留已知v1基线。没有修改 `.env` 或开启业务环境服务，没有业务库写入、模型发布、提交、推送或部署。本轮全部数据库写入只发生在任务私有本机 `_test` 库。

## 实际训练及结果边界

以既有240主体、480个任务输入的合成包重训24个候选，拟合零失败，保存全部64组评价，固定四项 `ridge:main_anchor` 供推理。完整参数包位于 `outputs/numeric-fullflow/2026-09-14/model-v1/bundle.json`，bundle SHA为 `1de39003420d8a61fe6436d6e0062ad9f921ceb0ef23ed9b9c3f26b9aacb33bc`。

当前工作区运行时门通过；对原480项可用任务输入逐项比对新JSON推理与训练器预测，最大绝对差为0。保留last_value作为报告比较基线，不将固定首个候选解释为已完成正式选优。

| 任务 | 挑战配对／标签分母 | Ridge MAE | last_value MAE |
| --- | --- | --- | --- |
| AD MMSE 6月 | 32／32 | 1.609704 | 1.437500 |
| AD MMSE 12月 | 27／27 | 2.465171 | 2.222222 |
| 脂肪肝 ALT 6月 | 26／26 | 15.107762 | 15.038462 |
| 脂肪肝 ALT 12月 | 27／27 | 19.980783 | 17.507407 |

四任务挑战误差均高于基线。以上是合成工程评价，未证明临床预测有效，不能把“链路完成”解释为“预测准确”。

第一次直接构建被旧来源包的源码摘要校验拒绝：`longitudinal_case.py` 后续新增能力投影字段，与原包冻结摘要不同。没有降低校验或修改原包；在 `.tmp/numeric-fullflow-20260914/model-training-source` 复制独立源码树，恢复精确匹配原SHA的5488字节schema，仅移除后加的能力投影，原输入规则未变。训练前证明全部实际推理实现文件与当前工作区一致，再执行原有严格来源验证与训练；训练后在当前工作区验证新bundle。详细证据为同目录的 `model-training-isolation.json`、`model-training-verification.json` 与 `model-training-result.md`。当前工作区直接重建这个旧包仍会严格拒绝源码漂移，不能声称该命令已无条件兼容所有历史包。

## 实际检索、说明和PDF

隔离库导入开发池输入测量记录，每病种8条，共16条。排除随访目标文件、同主体／依赖组、锚点后未知资料，保留病种、来源、代次与访问范围过滤。使用本机已有BGE-M3权重完成实际向量索引，未下载模型。两个病种的实际生成均记录向量和全文分支 `complete`，RRF各返回7条参考。

本轮真实DeepSeek调用共四次：首轮两病种运行，视觉和内容复核发现AD叙述误称“测量序列外推”；数值表没有错误，但该首轮说明不作为最终交付。明确补充“只使用锚点值及固定Ridge参数”的提示、增加错误算法说法拒绝测试后，重新运行两个病种完整链路。没有使用模板伪造LLM成功，没有隐式自动重试。首轮材料保留在 `outputs/numeric-fullflow/2026-09-14/browser`；最终材料在 `browser-final`。

请求模型为配置中的 `deepseek-chat`，服务实际响应模型名为 `deepseek-flash`，两者分别保存。最终说明经独立只读复核，与固定算法、检索状态和引用一致。结构及关键词校验不能全面证明自由文本语义正确，仍需保留此限制；参考材料是输入测量记录，不是指南、结局或疗效证据。

最终PDF均为3页，正文、计算值、目标日期、来源专用标签缺失检查和逐页视觉检查完成，无文字裁切或遮挡。页面以六位有效数字展示，后台保留完整值。

- [AD最终PDF](../../../outputs/numeric-fullflow/2026-09-14/browser-final/ad-download.pdf)：SHA `d0e7f3834403f567006881cabe4a530766ebce14f158d4dc4bdc5d159657f956`。
- [脂肪肝最终PDF](../../../outputs/numeric-fullflow/2026-09-14/browser-final/fatty_liver-download.pdf)：SHA `34048215e2e2cbc41680539a6d3f5edc711c0c248cbe517cacc266abd460dbec`。

实际API、Vite、Chromium及两个worker CLI验证：只读病例、类型化接单、模型/检索/LLM实际调用、保存发布、PDF准备及原件下载、重复下载字节相同、历史刷新回读、跨用户404、取消快照及关闭新接单不降级。最终浏览器无pageerror；结果、截图、保存报告JSON与逐页PNG位于同一 `browser-final` 目录。

## 本轮执行检查

以下批次有重叠，不将其简单相加为唯一测试总数。

| 检查 | 实际结果 |
| --- | --- |
| 模型子任务TDD、构建及CLI | 29通过 |
| 证据/叙述/种子与既有RAG | 48通过；其后数字与算法说明回归进入主任务检查 |
| 后端相关单元、发布/历史、模型/证据/叙述/类型生成 | 144通过 |
| v5真实隔离数据库事务及历史测试 | 13通过；外部适配器在该组使用fake，实际外部链路另行执行 |
| v4接单/worker/历史、归档约束回归 | 61通过 |
| 提示修订后的v2/叙述、RAG及新旧PDF单元 | 71通过 |
| 真实Chromium旧v4双病种/双来源PDF下载、损坏与原件恢复 | 4通过；真实来源为明确虚构契约fixture |
| 前端全套单worker Vitest / 契约 / DTO生成器 | 150 / 30 / 2通过 |
| 前端后续相关回归 | 28通过；显示精度改动后24通过（含新增精度保全用例） |
| 最终前端构建 | 通过 |

主任务初始测试命令包含不存在的文件名，零用例运行；修正路径后才执行上述测试。新v2测试fixture仍使用旧检索query字符串曾导致12失败/2通过，已按当前固定query契约修正，并保留真实检索内容校验。初始预期TDD红灯、训练来源校验拒绝和首轮LLM内容复核失败均不记为通过。

独立审查提出三项P2：历史依赖当前提示/过滤配置、接单未固定检索配置、数字文本拦截遗漏。均已修复，相关纯历史、队列漂移及Unicode/中文数字回归通过；最终静态复核关闭三项，未发现剩余明确P1/P2。复审未代替主任务实际测试及PDF视觉检查。

保留的环境警告包括Pydantic/FastAPI弃用、Vitest依赖注释、前端包体积和Playwright关闭时pending task/TargetClosedError诊断；实际退出状态、发布状态和原件字节另行核实，未升级依赖以隐藏警告。

## 保全与下一步

检查开始前986个既有文件，没有文件丢失；223个保护文件（其中201个既有输出）摘要保持一致，阶段二第5节逐字不变。没有改写旧合成算法、计算、候选训练及v1数值保存契约。任务私有API/Vite/worker及PostgreSQL已停止；默认业务开关保持关闭。

**本步完成后只剩第3步：固化统一隔离总验收入口，完成数据/模型版本切换演练和剩余异常场景（承接原E包）。** 真实数据到位后仍须按同一输入契约导入、训练和评价后绑定新模型版本；本次未验证真实临床效果、正式生产发布或生产容量。
