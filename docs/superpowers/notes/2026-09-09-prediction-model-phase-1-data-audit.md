# 预测模型重构阶段一数据盘点结果

日期：2026-09-09。盘点运行编号：`20260909T030551389Z`；首次归一化：`20260909T053524Z`；AI 来源核对：`20260909T063817Z`。状态：**两病种 Excel 归一化及一轮来源核对已完成，当前文件版本为 `clinical_source_normalized.v2`；未代签临床终点或患者独立性。**

现有文字和表格已清洗成每病种一个 Excel，便于查看和修订。依据用户最新要求，图片、OCR 和影像索引全部排除，图片缺口不再阻塞当前交付。下方原盘点记录保留历史事实；本次完成不代表已经得到可训练的独立患者数、有效纵向样本量或泛化性能。

本结果对应[阶段一实施计划](../plans/2026-09-09-prediction-model-refactor-phase-1-data-audit.md)，执行边界沿用[总领规格](../specs/2026-09-09-prediction-model-refactor-master-design.md)。用户已批准开始阶段一，并明确本人承担复核。

## 最终交付补充：用户修订与列精简

用户已说明对照原文补齐两份 Excel 的“年龄候选”和“性别候选”，随后要求删除所有工作表的“来源定位”列。列调整运行编号为 `20260909T084817Z`：两份工作簿各删除 5 列，共 10 列；来源文档、原文节选、记录编号和仓库外的原始定位审计材料保留。用户填写的内容未被重新提取结果覆盖，列名中的“候选”沿用旧名称。

该次验证记录显示：198,215 个保留单元格的值、类型和样式一致，600 个年龄／性别单元格（含表头）保留，13 个工作表的行数、表格范围和筛选等检查通过。此次提交前重新计算两个交付文件的 SHA-256，与该次验证输出一致。上述全量单元格检查是列调整时的记录，不是提交时重新执行的全量比对。

列含义已按用户要求在会话中解释，未新增说明文件或改写工作簿。用户日常只需查看 `AD数据集.xlsx`、`脂肪肝数据集.xlsx`；旧 CSV 与审计材料属于后台回查材料，无需再次全量填写。后续围绕选定预测任务核实具体缺口，不将整份人工重审作为阶段二设计的前置条件。

阶段一当前范围的清洗交付已完成；患者独立性、真实时间线和正式预测标签仍未因此自动通过。两份 Excel 位于仓库外，本次 Git 提交只保存设计、计划和去标识结果，不上传病例文件。

## 来源核对补充：按用户要求由 AI 核对来源

用户明确要求 AI 帮助审核。此次直接核对原始文件，产出两份 `v2` Excel。首次保存时 Windows 拒绝覆盖正在使用的原工作簿，因此曾暂存到“审核结果”目录；用户随后明确关闭文件，现已将两份 `v2` 写回原来的归一化目录，并核对最终哈希。任务生成的临时副本已清除，完整 `v1` 仅在私有审核目录保留备份；原始文档与既有封存审计材料未修改。

本轮修正及补充：

- 原表格的多时间点数值此前主要保留在正文，旧行级抽取不足以覆盖所有列。现在以 215 条单元格记录替换 19 条粗略记录，脂肪肝“指标记录”由 3,200 行变为 3,396 行。215 条中有 189 条表格检查记录、11 条参考范围、15 条未拆分访视的数值变化描述，不能将其全部视为新访视或患者。
- 纠正 7 处时间数字被提为指标值的问题：AD 4 处、脂肪肝 3 处。清除错误的数值字段，原数值表达、来源与修正原因保留。AD 表仍为 471 行，其中包含被标记纠正的记录。
- 病例表增加原文诊断摘录、病种文本归属、审核结果和依据；数值、诊断／随访和残余数字按参考、计划、讨论、剂量、时间等上下文归类。分类由本地可复现规则完成，不宣称逐条临床语义已由专家签核。
- 原文诊断段明确提及目标病种的来源条目为 AD 125 条、脂肪肝 121 条；其余保留疑似、多种表述、未明确记载或家系共有状态。这是文本记载计数，不是确诊患者或正式训练样本数，没有据此删除病例或重新诊断。
- FLD1 仍保留原始 1,592 行、24 列；AD 影像索引和 FLD2–4 仍不作为个体病例导入。原始资料可作回查或研究参考，不因当前用途不匹配而删除。

验证通过：14,349 个段落逐一与原始 OOXML 对应；3,652 条保留的旧指标记录逐字核对数值来源；215 条新增记录逐项核对源单元格及行列标题；两个 XLSX 共 211,155 个单元格完成回读比较；原正文、既有来源字段、公开数据完整保留；19 项人工构造的格式及语境反例通过。17 个源文件哈希未变，工作簿无图片、宏和外部工作簿链接，修改区域已渲染检查。未训练模型、未访问业务库，也未通过诊断标准重新裁定病种。

当前使用入口仍为两个 Excel 的“数据说明”“病例”和“指标记录”。后续模型工作需要围绕真实可用的时间与结果确定任务；原文不足以证明的患者身份、实际访视、结局时点继续保留缺口，不能由 AI 补造。

## 本次补充：两病种 Excel 归一化

以下计数记录首次归一化版本 `v1`；当前 `v2` 的修改与计数见上节。

用户选择 Excel 格式。两个文件均含病例、指标记录、病例正文、诊断随访、待核内容、数据说明；脂肪肝另含公开横断面表。所有明细文件和可复现辅助材料均留在仓库外，原盘点封存目录未修改。

| 内容 | AD | 脂肪肝 |
| --- | ---: | ---: |
| 医院来源病例条目 | 147（146 条单主体、1 条家系共有） | 151 |
| 合并完全重复正文 | 0 | 4 组各合并 1 条，来源全部保留 |
| 含数字表达式的指标记录 | 471 | 3,200 |
| 清洗正文段 | 607 | 1,504 |
| 诊断／随访原文记录 | 610 | 46 |
| 待核内容 | 1,652 | 4,346（含 2 个尚未归属病例的文本块） |
| 独立工作表中的公开横断面记录 | 0 | 1,592 |

上述病例条目不是已核验独立患者数。归一化统一空白及排版、指标名称、同一单位的写法和数值类型；区间、比较符及量表满分分列，原始数值与单位保留。正文保留表格行列标记。缺失保持空白，未确认时间不补造日期，不做 0–1 缩放、重新插补或临床标签代签。

FLD1 原始 24 列数值完整保留，年龄仍为分组；其包含 NAFLD 阳性与阴性，且来源已做均值填补，不能视为纵向进展队列。AD 影像索引以及 FLD2–4 汇总资料不作为病例纳入。

本次验证：回读两个 XLSX 的全部 185,187 个单元格，核对类型、内容、缺失、105 个日期及病例关联；逐行比对 FLD1 全部 1,592 行原始 24 列；核对非重复病例正文覆盖及 369 个表格单元格段落、未归属文字保留；17 个原始文件哈希未变。两文件均无嵌入图片、宏、外部工作簿链接或公式错误，13 个工作表均已生成预览，检查表头和说明页排版。验证通过，未进行逐条临床语义签核，也未运行模型训练或业务测试。

## 1. 实际执行范围（原盘点记录）

- 登记全部 17 个输入文件；四份医院 DOCX 的 SHA-256 与规格记录一致。
- 本地读取四份医院文档的 OOXML 段落、表格结构及图片引用，保留来源部件、段落／表格行列定位。
- 形成病例段、重复与家系关系、指标与数值候选、时间表达、诊断／事件提及及未匹配数字清单。
- 对现存图片执行离线 Windows OCR；由独立子任务核查图片关系和公开资料，由独立质量审查核对候选处理与来源覆盖。
- 全量核查两份 AD 索引、FLD1、FLD2、FLD3、FLD4，并核对 ADNI 与 Dryad 的当前官方说明。
- 产出仓库外的私有审计记录和供本人填写的 CSV 复核清单；本文件仅保留去标识汇总。

项目基线为 `29c09974140db7c78e5fc15ed0669ff373b0b0e4`，项目 Python 为 3.11.4。文档／表格工具使用已配置的本地依赖，未安装依赖或升级环境。审计辅助脚本保存在私有运行目录，属于本次离线提取的可复现材料，没有加入生产解析链路。

没有训练、下载或发布模型，没有访问业务数据库、调用外部 LLM、改动业务接口／前端或重算历史报告。原有未提交的文档改动得到保留。

## 2. 医院文档覆盖与病例主体

| 文档 | OOXML 段落数，含空段落 | 非空段落 | 表格 | 现存图片 | 顶层病例段 |
| --- | ---: | ---: | ---: | ---: | ---: |
| AD病例（1-73例）.docx | 1,713 | 1,643 | 0 | 0 | 75 |
| AD病例70例.docx | 2,505 | 2,413 | 1 | 13 | 69 |
| 脂肪肝相关病例（1-78例）.docx | 1,777 | 1,655 | 0 | 0 | 82 |
| 脂肪肝病例-2026.8.7.docx | 8,354 | 8,274 | 12 | 0 | 73 |
| 合计 | 14,349 | 13,985 | 13 | 13 | 299 |

段落数包含表格内段落，不是访视数。299 个顶层段中保留一份家系容器，另建立 3 个有明确主体标记的子段，所以 `cases.jsonl` 为 302 条结构记录。共有叙述没有复制给家系中的每个人。这些数字均不代表 302 名患者。

重新计算确认 4 组忽略空白后正文相同的脂肪肝病例段；这证明文本重复，不能单凭这一点确认患者身份。家系、同源重复及潜在跨文件同人仍需本人核验。所有 `patient_group_id` 目前为空，没有记录被认定可直接进入独立测试集。

完整性核查还发现：

- 两个非空文本块尚未归入病例主体，已经进入复核队列。
- 13 张现存图片都有正文引用；10 张经离线 OCR 得到候选文字，3 张没有读出文字。**13 张均未完成人工视觉或临床审核。** OCR 成功不代表图片中的医疗信息已读懂。
- AD 第二份文档有一项无效图片关系，被 15 处 XML 引用；未找到同一图形对象中的有效图片回退。需要核对这些位置的预期内容及其是否承载病例证据；不能据此断言缺了 15 张不同的临床图片。
- 本次没有验证医院 DOCX 的完整页面版式。机器文本覆盖、图片 OCR 与人工逐页检查分别记录。

## 3. 候选记录及其含义

| 项目 | AD | 脂肪肝 | 解释 |
| --- | ---: | ---: | --- |
| 顶层病例段 | 144 | 155 | 未完成跨文件身份与独立性核验 |
| 指标提及候选 | 815 | 5,685 | 包含无直接数值的提及，不是已确认检查 |
| 带数字表达式的候选 | 471 | 3,292 | 可能含参考值、范围、重复叙述或未确认情境 |
| 诊断／事件及随访核查记录 | 757 | 201 | 包含诊断提及及每病例的随访完整性核查项，不是事件人数 |
| 含残余数字的待核单元 | 1,652 | 4,412 | 可能是检查、时间、剂量、分母、参考范围或其他内容 |

合计保存 6,500 条指标候选，其中 3,763 条含数字表达式；另有 958 条结局／随访核查记录、1,537 条时间表达候选和 6,064 个残余数字单元。不同清单可能引用同一原文，不得相加为病例数或样本量。

所有指标记录均为 `needs_review`，观测状态仅为 `uncertain` 或规则识别的 `planned`；没有由程序自行批准的 `observed` 记录。未成功解析的数值、单位、标本或方法标为待核实，不能据此宣称原文没有记载。

复杂数字保留完整原表达式；不等式、范围、科学计数、千分位和量表分式不自动变为普通标量。图片 OCR 没有进入数值观测表。相对时间保留“前／后”和区间原文；锚点未核实前不生成标量时间偏移或具体日期。

本次机器提取经过格式反例修正，但不是经过临床验证的自动抽取模型。残余数字清单用于补漏；不能以“文件已经解析”代替完整人工转录核对。

以下事项目前**没有建立**，不以零样本或零事件代替未知：

- 独立患者数、真实完成访视数及实际随访间隔分布。
- 已核准的纵向观测对、当时可获得的信息边界 `known_by`。
- 事件相对于基线的位置、末次实际观察时间和固定窗口阴性标签。
- 可用于开发、校准或锁定测试的正式队列。

此前 AD 29 段／脂肪肝 23 段是不同规则下的初筛提示；本次指标提及计数也不与其等价。不能把计数增加解释为真实纵向样本增加。

## 4. 公开资料核查结果

| 来源 | 本次重新核实 | 用途判断 |
| --- | --- | --- |
| AD MRI CSV | 93,034 行、23 列、3,203 个不同 `subject_id` | 影像索引；缺少认知量表和诊断结局 |
| AD PET CSV | 10,205 行、8 列、2,364 个不同 `subject_id` | 影像索引；检查类型字段不能用作阳性／阴性生物标志物 |
| FLD1 XLSX、README | 1,592 行、24 列、年龄为分组编码 | 可考虑当前 NAFLD 状态的方法实验，不能直接训练未来进展任务 |
| FLD2 DOCX | 7 张研究汇总表 | 背景证据，非个体训练数据 |
| FLD3 六份 DTA、检索策略 | DTA 分别为 9、7、6、10、10、10 行，字段为作者、年份及 TP/FP/FN/TN | 研究级诊断准确性资料，非患者记录 |
| FLD4 PDF | 9 页、3 种 NHIS 统计表表达 | 人口汇总；广义肝病不能替代脂肪肝标签 |

两个 AD 索引合计有 3,234 个不同主体字符串标识，但仍未取得临床映射或目标标签；影像主体数不是本项目可用预测队列人数。

FLD1 当前文件没有空单元格，但 [Dryad 发布说明](https://datadryad.org/dataset/doi%3A10.5061/dryad.7d7wm3809) 明确披露使用 SPSS series mean 填补缺失。原始缺失位置没有恢复，FIB-4／ASCVD 仍是计算评分，不能当作经观察确认的器官事件。

FLD4 已完成 9/9 页文本读取及视觉核查；渲染有字体替代提示，关键标题、表格与脚注可读。两份公开 DOCX 完成 XML 文本／表格读取，没有验证原页面版式。未下载远端二进制文件核对哈希，也没有登录 ADNI 或取得新的临床表。

AD 补数清单包括 11 类需求：主体与影像映射、诊断变化、认知量表、日常功能、基线及随访／末次观察、人口学与治疗共病、影像定量及质控、PET 字段字典、研究阶段与版本、数据字典及取得记录，以及按任务需要选择的脑脊液／血浆生物标志物定量和检测信息。具体下载文件名和字段映射在取得官方字典后确认，不能根据索引猜测。[ADNI 数据说明](https://adni.loni.usc.edu/data-samples/adni-data/)、[临床评估说明](https://adni.loni.usc.edu/data-samples/adni-data/clinical-assessments/)

## 5. 验证与未完成事项

机械检查验证了 JSON/JSONL 可解析、记录 ID 唯一、关联目标存在、原始数值片段与来源一致、单位字符串来自原文，以及候选没有被升级为正式标签或独立患者。最终共执行 44,411 次来源引用检查和 30 项合成格式／语义反例检查，结果无错误。反例仅用于检查提取行为，没有加入真实数据。

独立质量审查另从原始 OOXML 核对 14,349 个段落及保存文本，核对复核队列覆盖、精确重复与家系不复制规则，并独立执行数值、时间、上下文等反例。该审查只评价机器候选盘点的质量，不代替本人核验患者或临床结局。

17 个输入文件结束时的 SHA-256 与开始清单一致。私有材料均保留在仓库外；仓库只新增此汇总并更新规格、计划状态。未运行应用单元测试、集成测试或完整开发基线，因为本阶段没有业务行为变更；也未进行任何模型性能评价。

正式训练准备仍有实际工作未完成：患者身份与跨文件关联签核、全量转录语义核验、时间锚点及真实观测对、疾病归属、事件及末次观察审核。它们有具体记录入口，**不是已经通过的验收项。** 图片和无效引用的历史未审核状态保留，按最新范围不再要求处理。

## 6. 私有交付物与本人复核顺序

私有运行位置由批准的计划和运行编号确定；此仓库文档不保存完整私有路径或逐人数据。

当前直接交付为 `AD数据集.xlsx` 和 `脂肪肝数据集.xlsx`，先从各文件的“数据说明”查看字段与处理规则，再在“病例”“指标记录”“诊断随访”“待核内容”中查看和修订。图片与 OCR 不在两个文件中。

原盘点的核心审计材料为 `source_manifest.json`、`coverage.jsonl`、`cases.jsonl`、`links.jsonl`、`observations.jsonl`、`outcomes.jsonl`、`reviews.jsonl`、`public-data-audit.json` 和 `audit-summary.json`。辅助材料保留原文定位、历史图片 OCR、残余数字、验证结果和提取脚本，便于回查及复现。

后续若使用原审计 CSV 签核，私有 `review-guide.md` 中的对应顺序为：

1. 填写 `review-cases.csv`，先核验同人、家系、病因和主体归属。
2. 核对 `review-observations.csv` 中适用病例的真实测量，补充时间、单位、标本、方法及计划／实际状态。
3. 核对 `review-outcomes.csv` 中的事件、基线关系和末次实际随访。
4. 利用 `review-residual-numbers.csv` 补漏，根据文字和表格定位回查原件。

在 `reviewer_*` 列填写决定和理由，保留记录 ID 与机器候选列。已有本人填写内容时，辅助脚本会拒绝直接重生成覆盖；本阶段没有自动导入或自动训练入口。

## 7. 下一阶段判断

| 病种／任务方向 | 当前结论 | 先完成什么 |
| --- | --- | --- |
| AD 认知或功能变化 | 需补证据／数据 | 本人核验真实量表与时间线，准备取得可对齐的公开临床随访资料 |
| 脂肪肝重复指标变化 | 需补证据／数据 | 核实主体、病因、治疗情境、同方法复查与实际间隔 |
| 脂肪肝肝硬化／肝癌事件 | 尚不能进入训练 | 确认新发事件人群、事件时点、末次随访及可靠的无事件证据 |

目前足以开始阶段二任务与评估冻结的计划设计，尚不足以冻结正式队列、样本量或评估标准。阶段二先根据两个最新 Excel 整理任务候选及具体缺口，来源能够回答的问题由 AI 继续核对，只有来源不能解决的身份、时间或临床判断交用户确认；不要求先全量重审阶段一材料。若数据不足，先设计补数或观察能力，不用合成行数补齐真实临床样本。
