# 统一数值报告第3步验收记录

日期：2026-09-15。状态：当前三步合成工程路线全部完成；正式真实数据冻结与临床评价条件保留。

## 范围与进度口径

本轮承接[统一方向](../specs/2026-09-14-unified-prediction-report-direction.md)及[第3步计划](../plans/2026-09-15-numeric-unified-acceptance.md)。目标是保留可重复的验收入口，演练合成数据和实际训练模型版本切换，保全历史报告/PDF，并更新两份总领文档。没有增加产品功能或更换报告模板。

当前三步工程路线的最后一步与原阶段二真实资料冻结是不同口径：原六任务中任务一已完成，任务二至六还有5类工作、23开放条目（15缺条件、8未执行）；36项中13满足。按原任务顺序，开放数为3、4、5、7、4。原第5节不因合成验收通过而改勾。

## 数据与模型

保留A：`outputs/synthetic-prediction-cases/2026-09-14-v1`、`outputs/numeric-fullflow/2026-09-14/model-v1`。

新B：`outputs/synthetic-prediction-cases/2026-09-15-switch-v2`；seed=20260915，两病种各120主体、其中挑战40。实际生成240主体、1333观测、480输入任务、480随访目标、66反例，质量门控通过。run_id=`syn-0aa14c3fe543c3b5`，数据内容SHA-256=`2a18dee7fe1dcb89b583c5c7091f44b45607f7d4c2e9a93112dae26303bab426`。

按原CLI顺序计算基线并实际训练B，输出 `outputs/numeric-acceptance/2026-09-15/calculation-v2` 和 `model-v2`。模型包SHA-256：A=`1de39003420d8a61fe6436d6e0062ad9f921ceb0ef23ed9b9c3f26b9aacb33bc`；B=`32b8069f92dab3e104f3668c3639cdc6e5461f5bedf61a4f7590ce2cef478215`。四任务参数组合均实际不同，两包当前runtime身份校验通过；未手改系数或旧来源摘要。

| B任务 | 配对数 | Ridge MAE | last_value MAE |
| --- | --- | --- | --- |
| AD MMSE 6月 | 28 | 1.175552 | 0.928571 |
| AD MMSE 12月 | 28 | 2.981086 | 2.678571 |
| 脂肪肝 ALT 6月 | 25 | 12.842630 | 11.556000 |
| 脂肪肝 ALT 12月 | 33 | 26.372483 | 24.863636 |

全部拟合无失败；固定Ridge仍未优于末次值基线。该表仅为合成工程评价，不是正式临床性能或模型选优结论。

## 本轮修改与运行条件

- 新增持久入口 `scripts/run_numeric_report_acceptance.py`、浏览器场景 `scripts/numeric_report_acceptance_browser.py` 和门控/进程测试。默认只读预检，实际执行需要双显式开关、已迁移干净隔离库、两份不同来源/模型、当前renderer和新输出目录。
- A沿用已验证来源导入；B从可信loader转换为当前版本包并使用 `seed_prediction_cases`，仍为synthetic。后端保留来源及数据版本，前端/PDF复用同一展示。
- 修复v5损坏测试的fixture假设：已有参考片段时原测试把`available`写回原值，没有篡改；现从publication读取原值并写不同合法状态，保留完整性拒绝断言。业务历史校验实现未改。
- 完整运行反复出现LLM说明校验拒绝后，收紧现有提示词：只解释已执行的方法，否定句也不得列举其他算法；说明限两段短文并要求提交前检查。保留数字/方法/医疗过滤的原拒绝规则，无自动重试、无模板替代。24项说明/v5回归通过；3份当前提示词已改变的真实保存报告仍完整性有效。
- 启动已有任务私有PostgreSQL实例的55449端口，另建新验收库；旧私有库仅用于回归，不写业务库。新库迁移0030，索引16条A开发池输入参考，使用已缓存BGE且关闭下载。独立构建当前renderer。

## 实际验证

执行证据保存在 `.tmp/numeric-acceptance-20260915`。

- 生成、基线计算、第二模型包构建CLI均退出0；两包runtime及四任务系数差异核对通过。
- 首批隔离集成77项：76通过、1失败（上述状态测试没有真正改变值）。修复后v5完整批次13项通过；原失败保留在 `integration.log`，修复记录在 `integration-v2-final.log`。
- 首次从根目录用 `-c backend/alembic.ini` 迁移因导入路径失败，未执行迁移；改为开发文档指定backend目录后迁移完成。模型、迁移和renderer日志各自保留。
- 新入口最终14项测试通过，包括实际UserOut邮箱校验、真实父/子进程退出、端口/输出/隔离库与外部调用门控。独立审查复现根先退出遗留子进程的P2；现Windows挂起创建进程、绑定现有Job后恢复，关闭Job清理全部后代，修前失败的真实回归修后通过。POSIX分支未实机验证。
- 真实PDF集成7项通过：两病种/两来源契约的原件交付及损坏恢复，另含真实Chromium发布、失租拒绝及发布回复丢失恢复。`real`来源仅为虚构测试fixture。前端代码未修改，本轮以实际页面验收；未重跑构建或刷新全项目基线。

### 失败与诊断记录

- `browser`：种子邮箱使用`example.invalid`，auth/me实际响应校验拒绝，尚无报告及LLM调用。改为`example.com`并增加真实schema回归。
- `browser-final`：修正邮箱时SQL漏引号，INSERT事务失败回滚，尚无报告及LLM调用。补齐引号，仍空的隔离库可以继续使用，但另开新输出保留失败记录。
- `browser-verified`：A脂肪肝实际完成并归档PDF；A AD受理后切换到B，执行仍使用保存的A模型，LLM说明触发`numeric_narrative_numeric_text_forbidden`，任务失败且未发布。共2次真实LLM调用。
- 对失败AD保存输入作一次新的诊断执行，捕获响应并通过原校验；仅生成内存publication，没有写回或修复原失败报告。原失败响应未保存，不能声称定位到具体文本；新响应见 `.tmp/numeric-acceptance-20260915/diagnostic-llm-response.json`。该次诊断未改提示词、内容校验或重试策略。

- `browser-complete`：A脂肪肝经1次真实LLM调用完成，但历史自动刷新后移除了条件性的“刷新查看”按钮，验收脚本等待该按钮超时。两处历史刷新改用常驻“筛选”动作，报告内容/身份/PDF断言保留，产品UI未修改。

- `browser-final-acceptance`：4次真实LLM调用，A双病种及B AD共3份报告/PDF通过；B脂肪肝因`numeric_narrative_algorithm_mismatch`拒绝发布。该响应亦未保留，已知拒绝类别但不推断原句。随后作上述提示词收紧，保留原验证条件与原失败报告。

LLM输出可能不满足严格契约，失败拒绝是当前实际行为。以上失败及诊断不计作完整总验收通过，最终新库完整运行另存`browser-acceptance`；不覆盖原失败，也不用多次调用挑选后填回原报告。



### 最终完整验收

最终入口退出0，`browser-acceptance/result.json`为passed。全部真实4次LLM报告均完成，vector/fulltext为complete并保存实际参考；A排队后切B仍使用A，后续B使用新数据/新模型，幂等原ID、取消、归属404、关闭接单、重启历史与PDF原字节通过，浏览器page_errors为空。

| 版本 | 病种 | 参考片段 | PDF SHA-256 |
| --- | --- | --- | --- |
| A | fatty_liver | 7 | `8439241fb5aa935c096bf924b2f83c16412f2a1fe94ad6eb8b2d73cba5e40fa6` |
| A | ad | 7 | `3b216db5fca400b3c0e39a06ffe2c3aa861a0168722d2de6488f525214314345` |
| B | ad | 7 | `0a70c3a1fc99913acfa62b6702a43f2fe3cf10df17cc32a20bb88584bc00345a` |
| B | fatty_liver | 7 | `da896dd8f208b541cb5c3c72963c4f464279e12d29a77e0f4a2486d8e8599ff1` |

最终四份PDF共12页逐页渲染核查，保存预测值/目标日期/单位与文档相符，无来源专用标签；页面/报告/PDF/逐页PNG及pdf-verification.json均位于最终输出目录。存在引用或参考段落跨页、末页留白较多的分页现象，内容可读，未重排已发布原件。独立复审的重要问题已关闭。

本轮前1059个已记录文件无缺失，250个旧输出摘要不变；只有本轮文档、状态测试及提示词的允许修改。原阶段二第5节逐字不变。55449/18060/15173均已关闭。

本轮共12次真实DeepSeek调用：失败路径及诊断8次，最终独立完整运行4次；未开启自动重试。既有Pydantic/FastAPI/fitz弃用及renderer预检Playwright退出警告保留，退出状态与发布结果分别核对。没有执行生产容量、跨平台或真实患者临床评价。

**下一步与剩余：当前三步工程路线剩0步。真实数据到位后按同契约导入审核、训练评价和版本绑定；原正式阶段二5类/23条条件仍保留，生产发布单独授权。**


## 限制与后续

本轮来源均为合成数据；真实来源契约的兼容回归使用明确虚构fixture。真实数据接入仍需同契约映射、事实审核、适用训练及独立评价。未切换业务环境或发布模型，未修改默认开关、`.env`、依赖或前端样式，未提交/推送/部署。
