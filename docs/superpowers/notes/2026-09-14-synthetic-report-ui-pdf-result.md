# 合成数值报告D包验收记录

日期：2026-09-14。状态：页面、类型化历史及专用PDF归档完成；下一步E包将新旧场景纳入持久隔离总验收启动器。

## 已完成范围

- 病例响应增加服务端验证的`engineering`安全摘要，不暴露完整来源包。来源缺失为空；来源损坏仍只读且禁用生成；开关关闭不回退旧报告。
- 工程病例采用纯只读摘要，隐藏输入编辑和保存；显式“生成合成数值报告”使用数值readiness及`report_kind`，幂等重试保留已保存类型，刷新只恢复观察。
- 新数值组件按保存文档展示匿名病例、锚点、实测图、6／12月目标日期／值／单位／不可用原因及来源审计。预测值不画作实测；历史、失败和取消输入快照识别新类型，未知或缺失文档拒绝旧模板回退与导出。
- 工程审计将`not_requested`显示为未执行临床标准／参考病例评价，不再显示“证据查询完成”。页面、历史和PDF保留合成数据及无临床有效性结论标识。
- PDF新增独立模板，渲染前核对保存文档、正文、数值与工程证据说明；缺少新文档、未知版本、篡改及不一致均拒绝。解除C包v3临时拒绝，继续使用现有准备、租约、原子发布、原件下载和恢复机制。
- renderer源码清单新增数值模板和渲染依赖。独立构建的manifest摘要为`789fe5aa17721addc89a729362f061e8474a4a12185c4bfb728d6a6c0777ee04`，位于`.tmp/synthetic-ui-pdf-20260914/renderer/`；使用既有受控Noto字体与Chromium151.0.7922.34，未覆盖旧renderer或已发布原件。

## 本轮实际验证

| 检查 | 实际结果 |
| --- | --- |
| 最终相关后端单元／真实进程，14个文件 | 118 passed，11.31秒 |
| C包执行／固定历史集成回归 | 27 passed；该轮同时运行的PDF用例出现错误指标断言，见下文修正记录 |
| 最终新PDF与旧准备／worker／交付／约束集成，5个文件 | 13 passed，31.93秒；与上行合计40项不重复集成通过 |
| 前端完整单元，21个文件 | 134 passed |
| 前端契约／构建 | 30 passed；构建成功 |
| 生成DTO与后端schema | 只读逐字比较一致，未手改生成文件 |
| 真实HTTP／Vite／Chromium／worker CLI | 两病种四任务生成、PDF准备及下载、历史刷新回读、跨用户拒绝、取消快照、关闭开关无降级均通过；pageerror为空 |
| PDF视觉检查 | 两病种真实归档各1页，中文、表格、日期、单位、来源说明和页码完整；逐页PNG已检查 |
| 独立只读复审 | 未发现遗留P1／P2；审查者未运行测试或连接数据库 |

真实集成使用任务私有PostgreSQL实例及实际spawn子进程／Chromium；比较下载SHA和完整字节，重复下载保持相同原件，跨用户返回404，损坏原件拒绝交付，使用同一原件备份恢复后字节不变。发布、下载与恢复均未改变报告正文、文档、指纹及原报告更新时间／下载计数；归档自身交付计数仍沿用现有机制。

浏览器验收由任务专用脚本`.tmp/synthetic-ui-pdf-20260914/run_browser.py`执行，使用真实鉴权路由、独立API和Vite，report worker及PDF worker均以CLI启动并完成后退出。按既有历史页“刷新查看”流程读取新增报告。受控取消没有启动计算worker，页面只显示保存输入而无伪造结果；重启隔离API关闭工程开关后生成入口消失。脚本在finally回收其服务进程树，不触碰其他服务。

验收中修正了测试本身的三处错误：脂肪肝指标误写为HbA1c（契约为ALT）、浏览器账号使用EmailStr拒绝的保留域名、请求断言漏掉既有空`model_options`。浏览器脚本也补上既有历史页刷新动作。上述失败均未计为通过，修正后对应验收通过。应用修正的工程审计文案有失败回归测试。未降低正文、权限或完整性断言。

日志保留于`.tmp/synthetic-ui-pdf-20260914/`。环境仍有Pydantic／FastAPI等弃用提示，Playwright退出阶段仍输出pending-task／TargetClosedError诊断；实际CLI退出、发布和PDF完整性断言通过，没有据此宣称运行时诊断已修复。前端既有依赖注释、混合导入和大包体积提示保留。

## 产物与保全

- [两病种PDF及逐页PNG](../../../outputs/synthetic-report-ui-pdf/2026-09-14/pdf/)为本任务实际隔离归档原件的副本。
- [浏览器结果与截图](../../../outputs/synthetic-report-ui-pdf/2026-09-14/browser/result.json)记录两病种下载摘要、取消和关闭开关结果；截图位于同目录。
- 本轮前922个文件无缺失，既有变更仅限本包实现、测试和进度文档；167个既有合成输出摘要未变，A／C算法及发布契约源码、模型制品保持原样。
- 阶段二第5节36项逐字保留：13项满足、15项缺条件、8项未执行。未用合成工程结果改变真实路线的放行状态。

默认工程开关关闭。没有写业务数据库、调用外部LLM、训练或发布模型、提交、推送或部署；私有数据库及本轮API／Vite／worker已停止。浏览器仅选取少量确定性场景，不代表临床验证、生产容量或完整E包验收；冻结240主体包全部可用，缺失值PDF／页面行为使用明确测试fixture验证，没有篡改来源包。

**下一步E包：** 扩展既有`run_operator_case_e2e.ps1`及对应测试，保留旧报告场景，将合成两病种四任务、来源负例、不可用输入及权限／持久任务／历史／PDF纳入可重复隔离总验收与统一记录。

真实路线剩余：A2事实及正式定义补齐 → A3正式组样与泄漏核查 → A4样本量及完整评价合同 → A5审核冻结 → 阶段三真实独立比较 → 阶段四正式接入 → 阶段五获准发布。合成数据规模增加不代替这些条件。
