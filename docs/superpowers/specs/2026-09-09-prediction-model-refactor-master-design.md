# 脂肪肝与 AD 预测模型重构总领文档

日期：2026-09-09；执行更新：2026-09-21。版本：0.97。状态：**阶段二、三、四合成工程全部完成；阶段四S1–S6实施6／6；阶段五本机隔离工程演示 S1–S4 已完成，实施4／5。** S6 以真实 API／独立 worker／真实检索／DeepSeek／Chromium 归档实际执行并退出0：5 份报告（含 B 排队后切 C 完成 v5、C 两病种、C AD 部分结果、真实排队取消）、5 次说明生成、5 份归档原件与下载字节一致，非所有者 404／doctor 403／幂等重放及跨配置历史保全均通过；runner 51项、前端168项／契约30项／构建通过。**原保留两项已于 2026-09-20 全部关闭**：①「既有 v5 行跨 0031 upgrade 保全」已补做往返演练并通过（真实 v5 发布行导入停在 0030 的一次性库 → 升到 head → 逐字段比对 5 张表完全一致）；②后端非 integration 回归的 2 项既有失败已修复（`database/schema.sql` 已同步 `ai_reports` 与 `operator_cases` 的列／约束漂移、cleanup specs 允许清单已登记 7 份设计文档），并新增全表等价性守护，整仓回归现为 **0 failed / 2299 passed**。2026-09-20 另按更替后的渲染制品完成一次复跑（`outputs/numeric-history-acceptance/2026-09-20-v2`，退出码 0），使当前工作区代码与当前验收记录一致。跨阶段遗留问题的逐条处置见[跨阶段审查问题清单](../notes/2026-09-18-prediction-phases-1-4-cross-phase-review.md)。仍未启用新包、未切换活动配置；阶段五尚未连接数据库、启动演示或调用外部 LLM，真实临床有效性未验证。依据：[阶段五设计](2026-09-20-prediction-model-refactor-phase-5-local-demo-release-design.md)、[阶段五计划](../plans/2026-09-20-prediction-model-refactor-phase-5-local-demo-release.md)、[阶段四设计](2026-09-16-prediction-model-refactor-phase-4-history-integration-design.md)、[阶段四计划与S5记录](../plans/2026-09-16-prediction-model-refactor-phase-4-history-integration.md)、[阶段三结果](../plans/2026-09-15-prediction-model-refactor-phase-3-offline-comparison.md)。

目标：在现有 AI 操作者纵向报告系统上，建立面向真实病例、可以验证泛化能力的预测模型。保留已经建设的报告、版本、任务与归档基础设施，优先重建真实观测数据、预测任务和评估方法，再按证据选择算法。

本文是后续分阶段设计和实施的总依据，不是已经完成的验证报告，也不是立即训练或发布模型的指令。阶段一资料整理及后续获批的阶段二合成工程范围均已完成；真实资料相关工作继续按本文的输入条件、交付物和验收标准推进。具体预测终点、时间窗和模型性能要求按阶段二的决策流程确定，不能由实施者自行猜测。

## 当前进度与剩余工作（2026-09-21）

**当前下一步：执行阶段五 S5“隔离集成、实际演示总验收与阶段交接”——该步需要用户单独授权操作隔离库与外部 LLM。** S4 已完成工程评价、安全诊断与 `release.json` 闭合：评价只从数据库／审计／文件事实复算并按本次 seeded 范围失败关闭，权限／角色／幂等由演示会话自己发起的固定检查记录，遗留 `starting`／`running` 记录只报告 `unconfirmed_stop`。 [阶段五设计](2026-09-20-prediction-model-refactor-phase-5-local-demo-release-design.md)和[五步计划](../plans/2026-09-20-prediction-model-refactor-phase-5-local-demo-release.md)的 S1–S4 已完成，当前实施4／5；S4 已实现可复算工程评价、会话内固定检查记录、安全诊断与发布记录闭合。当前没有精确 `TEST_DATABASE_URL`，8 项真实数据库集成用例按规则跳过，因此尚未实际连接或写入数据库（仅在既有隔离库上以事务方式执行并回滚过一次 SQL 可用性探针），也未启动服务／浏览器或调用外部 LLM。范围继续固定为本机、合成数据、隔离测试库和监督式工程演示；实际 `--apply`、生产部署、临床发布、提交或推送仍未授权。真实资料到位后仍须独立审核、适配、重新训练评价与版本绑定；合成总验收和本机演示均不等于临床准入。

**阶段二完成确认：按已批准的合成数据工程范围结项，阶段二剩余开发步骤为0。** 本机 `surgery_rag_test` 的前后端／worker和两病种PDF曾完成实测；随后本地另项展示调整记录确认最终两份两页PDF及历史原件核验，该记录不随阶段三提交。上述为已保存验收记录，本轮规划未重测服务或PDF。原正式冻结清单保留。

用户确认的当前路线是先用合成数据完成最终工程流程，再通过相同数据契约和版本切换接入真实资料。该路线跨越原阶段二至四的部分工程工作，不能再用旧阶段二清单估算还要开发多少轮。

| 口径 | 当前剩余 |
| --- | --- |
| 当前阶段二（合成数据工程范围） | 第1步统一入口/页面/PDF、第2步训练推理/RAG/LLM、第3步总验收与版本切换均已完成，当前路线剩0步 |
| 当前阶段三（合成数据离线比较） | T1–T6完成6／6，剩0步；完整实验、结果解释和后续交接均已完成，未切换模型 |
| 当前阶段四（合成候选接入） | S1–S6全部完成，实施6／6；隔离端到端总验收实际通过。下一页为阶段五（获准发布），真实数据不是前四阶段的前置条件 |
| 后续真实资料正式冻结 | 保留原36个条目的核验记录：13已满足、23开放（15缺条件、8未执行）。待真实资料到位后处理，不计入当前工程范围剩余步骤 |
| 后续真实资料应用 | 同契约导入和审核、适用训练/独立评价、版本绑定；正式发布须另行明确目标环境与范围 |

保留待真实资料阶段核验的开放条目为：任务二观测/时序与支持量3项，任务三任务卡/标签落实4项，任务四字典/样本/泄漏5项，任务五评估契约/门槛7项，任务六全量核验/版本冻结4项。工程完成不会自动确认真实临床定义、真实样本支持或模型有效性。

阶段二工程验收见[统一总验收计划](../plans/2026-09-15-numeric-unified-acceptance.md)；当前阶段三见[合成离线比较计划](../plans/2026-09-15-prediction-model-refactor-phase-3-offline-comparison.md)。下文有日期的历史记录保留当时状态；当前下一步以本节及最新执行记录为准。

## 1. 范围与基本决策

2026-09-09 用户补充明确：当前优先清洗原始文字与表格，交付每病种一个 Excel，便于查看和修订；图片、OCR 和影像索引不进入归一化数据集，图片复核不再作为本次交付的前置条件。该数据整理交付无需等待全部临床标签核准；其完成不等于正式训练数据验收。实际结果见[阶段一结果中的归一化补充](../notes/2026-09-09-prediction-model-phase-1-data-audit.md#本次补充两病种-excel-归一化)。

用户随后要求由 AI 帮助审核。已完成原文一致性、数值转录、表格行列和可复现上下文规则核对，修正明确的提取错误，审核结果及依据写入原来的两个 Excel（`clinical_source_normalized.v2`）。原文不足以确认的内容保留具体状态，不要求用户先审核整份数据才能继续整理。该审核不等同于重新诊断或确认未记载的真实随访。

用户后来对照原文补齐年龄与性别。工作簿保留来源文档、原文与记录编号。后续使用最新 Excel 作为修订入口，不用旧候选清单覆盖用户修订。字段释义按用户要求仅在会话提供。

### 1.1 本轮重构覆盖的内容

- 仅覆盖脂肪肝 `fatty_liver` 与阿尔茨海默病 `ad` 的 AI 操作者预测报告链路。
- 整理真实病例的重复观测、治疗上下文和结局，区分医院资料、公开队列、合成演示和测试数据。
- 分病种确定预测任务，验证纵向历史相对于末次检查的增量价值。
- 比较简单基线、现有模型以及适用的小样本方法，按独立评估结果决定是否采用。
- 必要时调整任务路由、输入契约、模型发布契约和报告展示，使不同任务可以分别成熟。

### 1.2 保留的基础能力

保留病例归属、角色与疾病权限、内容过滤、来源追溯、版本审计、输入快照、持久任务、幂等、取消、超时与租约机制。历史报告继续读取保存的输入、预测、证据和正文；PDF 下载继续交付已经发布的归档原件。

不将本次工作扩展为聊天 RAG 重写、通用模型平台、第三病种接入或原始医学影像训练工程。已有合成数据和模型制品保留其来源与历史身份，不原地改成真实训练数据。

### 1.3 路线取舍

| 路线 | 决策 | 原因与前提 |
| --- | --- | --- |
| 保留报告系统，重建真实数据、任务与评估 | 首选 | 能复用已有工程能力，逐项验证模型改动的收益 |
| 针对随访不齐的事件任务采用生存分析 | 条件采用 | 需要可靠事件时间、末次随访与删失定义，由数据决定 |
| 直接换成复杂时序网络或从零元学习 | 当前不优先 | 尚未证明现有数据规模、相关任务集合和真实结局支持这种复杂度 |

“模型可加载”“离线指标合格”“真实病例泛化”“具备临床用途证据”分别判断，不互相替代。不以报告生成成功作为预测模型验收标准。

### 1.4 合成工程验证先行（2026-09-14新增授权）

用户明确要求先合成病例验证系统，等真实病例到位后逐步替换；随后要求先评估旧生成器是否适用。因此新增工程验证路线，不等待外部数据申请，不把此前真实数据尚缺条件解释为禁止生成软件验收病例。

[本次生成器评估](../notes/2026-09-14-synthetic-generator-assessment.md)已完成：保留旧样例和验收基础设施，重新设计主要合成病例逻辑及场景。每病种30人时仅有5套AD／7套脂肪肝指标轨迹，扩到300人不增加种类；仅用任务路由和复查次数即可命中该批测试事件标签。工程验证必须检验这些问题，不能只追求模型高分或报告生成成功。

这条路线覆盖本次双病种AI操作者预测报告系统，现有链路与拟议6／12个月MMSE／ALT任务分别验收，不扩展为聊天RAG重写。合成结果标为工程用途；临床人群、真实随访资格和性能门槛仍按真实证据确定。真实病例以后通过独立新版本接入，旧合成病例和已保存报告／PDF保持原身份，不能原地改成真实来源。

[新生成器设计v0.1](2026-09-14-synthetic-prediction-cases-design.md)已将场景、数据结构、随机机制和验收预期具体化。首个实施包限离线生成器与质量检查；新四任务评分器和应用链路改造分包推进。当前API拒绝空数值／单位、旧AD事件路由不支持痴呆阶段等事实单独记录，不为通过新样例降低现有门槛。设计中的日期算法、模拟参数和工程池不构成临床容差、样本量或正式分区批准。

[首包实施结果](../notes/2026-09-14-synthetic-prediction-cases-result.md)已完成：新生成器、固定反例、独立质量门禁及安全导出可运行；默认AD／脂肪肝各120人，唯一指标序列118／120套，扩量后235／240套。真实旧生成器作为失败对照仍为5／7套且换种子不生效；没有以增加ID冒充多样性。

[四任务离线计算结果](../notes/2026-09-14-synthetic-prediction-calculation-result.md)随后完成：组样、保持当前值／历史外推基线、患者等权评分、依赖组配对区间与可复算结果包已实现，155项相关测试通过。合成挑战池的简单历史外推误差更大，失败结果照常保存；临床门槛和前提未满足，性能状态均不可判定。该计算包未训练候选模型；后续候选实施见下段。

[合成候选与D03设计](2026-09-14-synthetic-prediction-candidates-design.md)已获确认并完成[实施与验收](../notes/2026-09-14-synthetic-prediction-candidates-result.md)：固定Ridge与小型随机森林，仅开发池拟合；24份模型、64组比较及新进程复算通过，208项不同相关测试通过。现有120人／病种可运行本次离线流程，但D03训练仅29—38人；另9次规模／种子生成质量核查通过，未训练更大队列。上一轮建议的规模／种子稳定性实验见下段，之后最小应用及隔离全链路验收。真实路线仍需A2／A3、A4样本支持／分区／门槛、A5冻结，再进入阶段三真实独立比较、阶段四接入与阶段五获准发布。工程结果不代替真实准入，详细剩余步骤见本轮验收记录第6节。

[规模／种子稳定性实验](../notes/2026-09-14-synthetic-prediction-stability-result.md)已按预先固定协议实际运行：每种子每病种1200人，开发内固定160验证、400原挑战，训练64／320／640组嵌套。9单元、216模型全部拟合成功，59项相关测试通过，独立重算通过。扩量后的验证／挑战主分支平均误差分别5／8和6／8组合改善，但最大档各8个三种子平均MAE仍高于保持当前值；D03两个角色都没有组合在三个种子中全部为正。下一步制定最小应用接入方案及隔离验收条件，真实路线及旧报告身份保持。

[第3步统一总验收](../notes/2026-09-15-numeric-unified-acceptance-result.md)已完成：持久隔离入口、新合成数据及实际训练模型、两病种四报告/PDF切换验收已通过。A排队任务在当前配置B时仍执行保存A，幂等与历史/原件保持；当前合成工程三步全部完成。原阶段二真实资料审核/定义/样本与评价条件保留，后续按同契约导入、适用训练评价和版本绑定，不重新开发展示流程。

## 2. 现状与证据边界

本节依据 2026-09-09 本次会话的代码与文件审查；代码基线为 `29c0997`。后续实施前应检查变更，不能将本节自动当成届时仍成立的环境事实。

### 2.1 已核查的当前实现

当前主路径是：保存病例与访视 → 受理时固定输入和生成上下文 → worker 加载固定模型套件 → 构建表格特征并推理 → 读取标准与相似病例 → 规则解释 → 结构化报告 → 保存和独立 PDF 归档。

- 当前报告主路径没有让外部 LLM 计算预测分数；相似病例是预测后的证据补充，没有参与当前分数计算。
- 当前活动模型数据快照每病种含 30 个合成患者，患者划分为训练 18、验证 6、锁定测试 6；该快照的真实训练患者数为 0。
- 已保存的三个结局模型测试 AUC 为 1.0；当前阶段与趋势模型相关分类指标同样为 1.0。这些是合成演示评估记录，不是实际临床泛化结果。
- 演示生成器的同病种患者共享固定访视节奏和高度相似的轨迹、事件位置。因此，高分主要反映模板识别能力是合理推断，尚未通过专门消融实验量化。
- 特征主要为末次值、均值、变化、斜率、缺失与访视统计。结局和阶段任务采用 365 天窗口，趋势任务为不固定间隔的下一次随访方向。
- 当前趋势标签统一采用 5% 相对变化容差；当前模型未校准，风险层级来自程序中的分数分段。
- 当前信号解释区分观察、参考规则与模型输入关系，没有计算个体模型贡献。
- 当前 release set 要求病种固定任务集合完整，readiness 依赖统一的最低访视数和适用结局路由。任务独立成熟是拟议变更，不能直接通过放宽现有检查实现。

本次会话此前运行的特征、预测契约、信号解释、输入审计测试共 67 项通过。该结果只证明对应软件行为；不是本文件编写时重新运行的完整基线，也不证明模型准确率或生产环境容量。

### 2.2 原始病例资料

用户确认四份文档来自医院原始病历或检查报告。本次审查确认文档中存在重复测量、相对随访时间、治疗变化和诊断描述，但未逐项核验其底层医疗记录。

| 文档 | 文件 SHA-256 |
| --- | --- |
| AD病例（1-73例）.docx | `46718fec6cef8df183ac0dffa1c5fbe6e412843432adc62514949cfdcf533a29` |
| AD病例70例.docx | `8b8c77719dc66f2f0a5dbfd200a7b10487f2ff9e7cb1b1c57da16a50b17e0e80` |
| 脂肪肝相关病例（1-78例）.docx | `8e5cd1158512b9806d84ca83517cc444186bf5aacd17e0c0ab70b767d32b2955` |
| 脂肪肝病例-2026.8.7.docx | `3542d340059af7fddbc6ce867ef2bc970a1bb917f4cf263b133b791fed45a372` |

这些哈希在编写本文件时读取原文件计算。文件位置由本地受控来源清单记录，不把原文、患者映射表或可识别健康信息提交到仓库。

初步发现：

- 四份文档并非全是单次快照，应重新提取原文明确记录的真实时间线。
- 存在重复编号、家系子病例；脂肪肝文档中发现 4 组忽略空白后正文完全相同的病例段。编号、文档段数和独立患者数不能混用。
- AD 内容中含其他痴呆病因或混合病因；脂肪肝内容中含其他肝病病因和复杂共病。适用人群需逐例复核。
- 规则初筛定位到 AD 29 个、脂肪肝 23 个带时间线索和重复数值的候选病例段。该计数可能包含重复和不同检查情境，也可能漏检，只用于安排复核，不能作为训练人数或样本量依据。**（2026-09-20 补充：** 阶段一记录已明确说明，这两个数字是**不同规则下的初筛提示**，与阶段一实际交付的指标提及计数**不等价**，见[阶段一结果](../notes/2026-09-09-prediction-model-phase-1-data-audit.md)。引用时须连同该说明，**不得单独作为阶段一产出引用**。**）**
- 已确诊疾病、短期治疗后变化、未来新发事件属于不同任务。未记载进展不等于完整随访后未发生进展。

### 2.3 公开资料的用途

| 来源 | 本次核查事实 | 本轮用途 |
| --- | --- | --- |
| AD/All_Subjects_Key_MRI_27Aug2026.csv | 93,034 行，3,203 个受试者；为影像索引，没有认知量表和诊断结局列 | 保留影像关联用途；优先补齐相应临床随访表 |
| AD/All_Subjects_Key_PET_27Aug2026.csv | 10,205 行，2,364 个受试者；`tau_pet`、`amyloid_pet` 为检查类型标记，不是定量生物标志物 | 不把索引行数当患者数，不把类型标记当检查阳性或定量值 |
| FLD1 的 XLSX 与 README | 1,592 个独立 ID、24 列；NAFLD 阳性 973、阴性 619；年龄是分组编码 | 可作当前 NAFLD 状态分类的独立方法实验，不作为进展主训练集 |
| FLD2/Supplemental_file.docx | 睡眠、NAFLD 与新发糖尿病研究的附录汇总表 | 保留研究背景，不作为个体病例训练数据 |
| FLD3 的 6 份 DTA | 每份 6～10 行，作者、年份和 TP/FP/FN/TN | 属于研究级诊断准确性汇总，可作证据，不作为患者数据 |
| FLD4.pdf | NHIS 2018 人群疾病统计表，肝病分类不是专门的脂肪肝结局 | 不纳入患者预测训练 |

FLD1 发布页说明缺失值已采用均值填补，因此不能将当前无空值理解为原始测量完整。其 FIB-4 和 ASCVD 列为计算评分，不能当作真实纤维化事件或真实心血管事件。年龄组不转换为伪造的精确年龄，也不以组中点冒充原始年龄。

## 3. 目标数据与预测契约

### 3.1 真实观测与结局

先形成离线、受控、可追溯的数据版本，再考虑与业务数据库衔接。优先复用已有数据集清单与哈希机制，不为首次盘点迁移或写入业务数据库。

每条观测需要能表达：匿名患者与来源组、来源文件及原文定位、指标与数值、单位、样本类型、检测方法或量表版本、观察时间及精度、当时已知治疗、缺失原因、审核结果。患者／家系／同源病例映射仅存放于获准的私有位置。

- 保留绝对日期、相对时间、仅年份、时间区间和未知时间之间的区别。能明确“距基线 14 个月”时保留相对关系，不生成任意具体日期。
- 日期不精确时，只用于任务允许的时间表达；不能据此强行生成严格 365 天标签。事件仅能定位到区间时，按任务规定使用区间删失方法或排除相关样本。
- 同一患者多次入院、重复整理、家系关系和跨文件重复在划分前处理；无法确认独立性的记录不进入锁定测试集。
- 当前阶段必须对应预测锚点，不能从最终诊断倒填。未来访视、未来治疗和未来结局不进入特征。
- 原文缺失值保持缺失。统计插补只能在获准的模型预处理内拟合，并记录策略；不回写为真实检查结果。
- 结局明确区分已发生、完整窗口未发生、观察不足、不适用和待核实，并记录确认依据与末次随访。
- 合成数据仅继续用于流程、异常和排版测试，不计入真实模型正式评估；测试集不由真实病例扩增或复制充数。

每个正式纳入的目标标签都须有复核记录。自动提取可以产生候选，但不能自行批准临床标签。需要专业判断的疾病归属、事件定义与临床变化阈值由用户指定的具备相应能力的复核者确认。

### 3.2 每个任务必须在训练前固定的内容

| 契约内容 | 必须回答的问题 |
| --- | --- |
| 使用场景与人群 | 给谁使用，基线处于什么阶段，哪些病因和共病纳入或排除 |
| 预测锚点与信息边界 | 在哪次访视预测，当时哪些信息确实可获得 |
| 目标与时间窗 | 预测什么，多久以后，允许什么随访误差和日期精度 |
| 标签与删失 | 如何确认事件、未发生、观察不足和区间事件 |
| 输入条件 | 必需指标、单位、检查平台、最低历史量和允许缺失范围 |
| 治疗语境 | 预测基于何种已知治疗状态，如何处理预测后的干预变化 |
| 输出与适用范围 | 数值、方向、分类分数或概率分别代表什么，何时不估计 |
| 评价与准入 | 主指标、基线、误差容忍、精度要求和失败条件是什么 |

训练样本数量与推理最低访视数分别定义。基线加一次未来复查可以形成基线预测的训练对，但不等于具有两次历史观测加未来标签的纵向样本。不能为了通过现有三次访视门槛补造记录。

## 4. 两病种的建模路线

### 4.1 AD

优先补齐公开队列的诊断、访视日期、认知／功能量表和人口学数据，再核对与本地病例的共同特征。ADNI 的具体下载表、字段和适用阶段以当时官方数据字典及使用条件为准；本地影像索引不能代替临床表。

阶段二在以下候选中确定一个首发主任务：

1. 痴呆前人群的固定窗口诊断转归；应区分临床痴呆与 AD 病因定义，不将所有痴呆自动视为 AD。
2. 已确诊 AD 人群在固定窗口的认知或功能变化；量表、时间窗和变化含义由可用数据与使用场景决定。

不预先承诺两个任务同时上线。若本地病例以已确诊 AD 为主，应优先评估第二类任务能否满足真实使用需求；不能仅因当前代码存在转归任务就沿用不匹配的人群。

输入设计以真实可得性为基础。脑脊液与血浆、p-Tau181 与 p-Tau217、不同量表版本不直接合并。评估由基础临床指标构成的模型；增加生物标志物或影像定量特征时，使用独立且明确的输入契约，验证覆盖率和增量收益，不通过大规模插补假装检测齐全。

公开队列用于开发与独立留出评估，本地病例用于迁移适用性验证。若本地数据参与训练或校准，应另留未参与适配的患者评估。无法形成可靠本地测试集时，只报告外部队列结果与本地适用性限制。

### 4.2 脂肪肝

先盘点真实生化、肝硬度或其他有效重复测量，确认病因、治疗条件和复查时间。短期指标变化不能被直接命名为纤维化进展或肝癌风险。

- 若重复测量支持：选择一个有明确时间窗和足够审核样本的指标变化任务，先验证其相对简单基线的收益。
- 若存在足够可靠的进展事件及无事件随访：再设计肝硬化／肝癌事件任务；随访不齐时评估生存分析，不能将观察不足当阴性。
- 若现有资料不能支持上述任务：完成真实纵向观察、标准解释与数据缺口说明，暂不接入新的统计预测。该阶段成果标为“数据整理／观察能力完成，预测未具备条件”，不能宣称预测重构完成。

FLD1 的当前 NAFLD 状态分类仅作为单独说明用途的可选方法实验；既有用户已经管理脂肪肝病例时，不为了使用该数据而改变主要产品目标。不以 FIB-4 公式复现冒充纤维化预测。

## 5. 小样本方法与评估规则

### 5.1 候选方法

优先建立可解释且可复现的简单基线，再与现有随机森林、适用的树模型比较。根据任务分别使用正则化分类／回归，事件时间任务再考虑生存模型，不要求所有任务使用同一算法。

TabPFN 等表格基础模型可作为离线对照候选。其已有研究结果仅支持开展实验，不代表在本项目中一定优于简单模型。涉及新依赖、模型权重、许可、内存或运行时不兼容时，先在阶段计划中说明；不为实验静默升级项目固定环境，不通过外部推理 API 上传病例。

迁移学习优先考虑同病种、可对齐输入和目标的公开队列。少量本地病例可用于有限参数适配或校准，但必须保留评价独立性。当前不从零建立跨脂肪肝和 AD 的元学习框架。

LLM 的少样本提示可用于辅助信息提取；如需实际调用，需另行明确数据处理方式和授权。LLM 不补造标签、检查值或随访，不替代数值预测与人工审核。

### 5.2 必需的对照与防泄漏措施

- 分类／事件任务比较基本信息、末次指标、增加历史特征三组；连续指标任务比较维持末次值、合理的历史趋势外推和候选模型。
- 增加访视次数、观察跨度等随访节奏对照，检查模板或流程信号；不能把流程特征的重要性直接解释为疾病机制。
- 同一患者的所有窗口放在同一分区；家系及同源重复按分组处理。多个窗口不作为多个独立患者报告样本量。
- 特征选择、插补、标准化、调参、阈值选择和校准均限制在开发数据内部；不得读取锁定测试结果后反复调优并继续称其为独立测试。
- 小样本内部评估采用适用的分组交叉验证或重采样；能否保留独立测试集按事件数、样本量及估计精度决定，不机械照搬 18/6/6 比例。
- 数据支持时补充跨时间或来源验证。时间划分须说明评价新患者还是既有患者后续访视，不混称两种泛化能力。
- 特征消融、比较人群和指标预先固定；不得在结果出来后只挑有利病种、窗口或亚组报告。

### 5.3 性能、不确定性与停止条件

分类任务评价判别、校准和预定阈值表现；连续任务评价误差、偏差及适用的预测区间覆盖；生存任务使用适用的时间相关指标并检查删失假设。Brier 分数不单独作为校准充分证明。

报告实际独立患者数、事件数、缺失构成和患者／家系层面的不确定性。模型适用范围内的年龄、阶段、检测条件和缺失程度应有分层检查；支持不足的亚组标为无法可靠评估。

不在本总领文档中凭空指定 AUC、误差或样本数门槛。阶段二必须在查看锁定测试结果前确定主指标、基线比较方式、容许误差及估计精度要求，形成有复核人的任务卡。没有完成这些定义，不进入正式训练评价。

不能稳定优于基线、校准或精度不合格、缺少独立评价、样本支持不足时，不进入模型发布阶段；保留失败结果和适用性结论。不能通过增加合成患者、删除困难样本或降低既定标准制造通过。

## 6. 系统衔接与兼容原则

以下为需要在阶段四详细设计的变更边界，不是现有系统已经具备的能力。

1. **任务级输入要求。** 将最低历史量、输入字段、单位和人群限制绑定具体任务；旧模型仍遵守旧契约，不统一把三次访视门槛改成一次。
2. **任务集合的版本化。** 从病种固定完整模型套件逐步转为版本明确的能力集合。声明未提供某任务，与已声明制品丢失或损坏必须分开；前者显示不可用原因，后者继续完整性失败，不允许静默降级。
3. **报告受理条件。** 受理时固定实际选择的任务和版本。仅观察报告与预测报告的名称、完成标准和缺失说明需明确；报告内容生成成功不代表所有预测成功。
4. **输出语义。** 不继续将未验证分数分段称为已验证临床风险层级。概率、分类分数、观察方向和未来预测分别表达；数值预测及区间只在模型支持且验证后输出。
5. **解释边界。** 规则信号与模型贡献分开。后续增加归因也只能解释模型行为，不能解释为因果关系。现有阶段单调限制和统一趋势容差必须按新任务重新审查，不能当作无须验证的临床事实。

   **2026-09-20 限定范围复核结论：** 本项在阶段四**未被认领**（「单调／趋势容差／解释边界／归因」在阶段四设计与计划中命中数均为 0），且经复核确认三项**当前仍存在**的事实：

   - **(a) 规则与模型贡献未分离。** `longitudinal_prediction.py:300 _monotonic_stage_projection` 以「疾病阶段不得回退」规则替换模型预测，并把候选分数过滤为不低于当前阶段秩；`StageProjection`（`schemas/longitudinal_report.py:12`）只保存规则后的 `likely_next_stage` 与过滤后的 `stage_candidates`，**模型原始预测被丢弃**。即模型若预测「好转」，该输出不会留下任何痕迹。
   - **(b) 该规则未向用户披露。** 「单调」一词仅出现在实现文件中，报告、模板与前端均无；报告把规则结果标注为「**最可能阶段**」，读者无从得知曾发生规则覆盖或有候选行被过滤。
   - **(c) 统一趋势容差未披露，且存在单位不一致。** 容差为硬编码 `0.05`（`longitudinal_trend_training.py:92`，`tolerance_version="relative_tolerance.v1"`），报告只显示「方向」而不显示该带宽；`direction_label` 在 `current == 0` 分支用**绝对差**与该**相对**容差比较（`if abs(delta) <= tolerance`），与其余按相对比的分支语义不同——CDR 的正常值恰为 0，该分支会得到与其他指标不一致的「stable」带宽。

   子要求「归因只能解释模型行为」目前无对应功能，记为前瞻项。以上三项已登记为待办（见[跨阶段审查问题清单](../notes/2026-09-18-prediction-phases-1-4-cross-phase-review.md) D3／D4）；在新任务定义（真实资料阶段）之前，该约束仍在线且未经临床验证，**不得据此宣称已验证的临床事实**。

   **2026-09-20 处置：**

   - **(a) 已修复。** `StageProjection` 增加可选字段 `raw_likely_next_stage`（`schemas/longitudinal_report.py`），**仅当规则把模型结论改到不同阶段时**留存模型原始输出——纯 `stay_X` 与 `X` 的写法归一不算覆盖，故**非空即代表发生过规则覆盖**；旧保存文档无此字段仍可读取。报告正文（`report_document_builder.py` 第 5 节）新增一句披露该规则及其可核对位置，满足「规则信号与模型贡献分开」。回归：`test_v3_stage_projection_records_the_model_output_the_rule_overrode`、`…_omits_raw_for_pure_stay_normalization`。
   - **(c) 已加注并补测试，未动语义。** `direction_label` 的 `current == 0` 分支已注明它是**退化处理**（无法算相对变化，于是把同一 `tolerance` 当绝对阈值用，语义与相对分支不同），行为由 `test_direction_at_zero_uses_absolute_tolerance` 固定。此类有序列量表（CDR 正常值为 0）的正确容差**属临床判断，标注待专业确认**；不动语义的理由是改它会波及冻结模型工件中记录的 `relative_tolerance.v1`。
   - **(b) 归因功能仍不存在**，保持前瞻项。
6. **历史兼容。** 使用新的数据、任务、特征与报告契约版本，不重写旧快照，不重算旧报告。新版本必要的 schema、前端生成类型、调用方及兼容读取同步更新。
7. **运行授权与证据状态。** 沿用并明确当前 active 指针的运行授权语义；现有 artifact 的 `production_enabled` 不是单独运行开关。临床证据状态、校准状态和真实数据构成应真实披露，不通过翻转旧字段伪造证据升级。
8. **上线与回退。** 新模型先离线验证、隔离环境验收，再按授权切换活动版本。切换只影响新任务；回退不删除新旧制品或历史事实。仅在接受旧输出限制的范围内回退，不能将演示模型包装为真实预测备用模型。

   **归属澄清（2026-09-20）：** 本节前言统一写作「需要在**阶段四**详细设计」，但本项的**上线切换**一半属**阶段五**（§8.2 表格亦记作「阶段四、五」）。以本注为准：**阶段四只负责契约结构与隔离验收，上线切换留在阶段五。**

复用的主要入口如下，具体改动文件由阶段实施计划根据届时代码确定：

| 职责 | 当前入口 |
| --- | --- |
| 数据与标签 | `longitudinal_dataset.py`、`longitudinal_dataset_export.py`、`longitudinal_group_split.py` |
| 特征与训练 | `longitudinal_features.py`、`longitudinal_model_training.py`、`longitudinal_stage_training.py`、`longitudinal_trend_training.py` |
| 评估与发布 | `longitudinal_model_evaluation.py`、`longitudinal_release_set.py`、`longitudinal_model_registry.py` |
| 适用性与推理 | `operator_case_readiness.py`、`longitudinal_task_routing.py`、`longitudinal_prediction.py` |
| 固定上下文与报告 | `report_generation_context.py`、`report_input_audit.py`、`report_document_builder.py`、`report_publication.py` |

以上服务位于 `backend/app/services/`。报告契约来源为 `backend/app/schemas/report_document.py`；worker 入口为 `backend/app/workers/report_execution.py`。

## 7. 分阶段交付与验收

各阶段先写范围明确的实施计划。任务定义、临床标签和准入标准等实质选择交用户确认；已明确范围内的常规实现和验证不反复索取许可。训练、数据下载或导入、业务库迁移、发布分别按对应阶段的明确授权执行，不将本草案视为这些操作的授权。

两个病种允许按数据条件分别通过验收，不因其中一个缺数据而伪造其完成状态，也不阻塞另一病种可独立开展的工作。

下表保留正式真实资料路线的验收条件。用户获准的合成先行路线按第1.4节和当前阶段计划执行：阶段三已完成合成离线实验与交接，可以继续阶段四的合成工程设计、必要接入和隔离验收，不要求等待真实数据冻结。进入合成阶段四不等于满足正式临床准入，也不等于候选可以直接加载；以后替换真实资料时，仍须完成表中的数据、评估及发布条件。

| 阶段 | 工作与交付物 | 验收及下一步条件 |
| --- | --- | --- |
| 阶段零：总领文档确认 | 审核本文件的目标、路线、约束与执行顺序 | 用户明确批准；批准前不实施模型重构 |
| 阶段一：真实数据盘点 | 受控来源清单、重复／家系映射、候选观测与结局、人工复核清单、各病种可用性报告；列明 AD 缺少的临床表 | 原文定位可追溯；独立患者与候选窗口分别计数；来源、时间精度、单位和未知结局保留；给出数据能支持与不能支持的任务。先完成现有本地文件盘点，外部数据取得条件单列 |
| 阶段二：任务与评估冻结 | 每病种首发任务卡、标签审核记录、数据字典、患者分组策略、评估指标与准入标准、受控数据版本 | 目标人群、窗口、标签、删失、输入及评价均明确；用户确认实质任务选择，必要临床定义完成专业复核。数据不够时转入补数据或观察能力，不进入正式模型训练 |
| 阶段三：离线基线与候选比较 | 基线／纵向增量实验、适用的小样本候选、独立评估、模型说明和失败分析 | 使用冻结版本且无泄漏；结果完整披露；只有满足阶段二标准的模型才进入接入设计。候选不合格不改活动指针 |
| 阶段四：契约与报告接入 | [阶段四设计](2026-09-16-prediction-model-refactor-phase-4-history-integration-design.md)与[阶段四六步计划](../plans/2026-09-16-prediction-model-refactor-phase-4-history-integration.md) | **【合成路线】已完成 S1–S6（实施6／6）**：报告、页面／打印、新renderer 与真实 API／worker／RAG／LLM／归档链路均已验收；未切换活动配置或发布模型。**【真实资料】本行的验收条件尚未开始**——真实候选未接入、未评价。原保留两项已于 2026-09-20 关闭：迁移保全往返演练已补做并通过，后端回归已全绿 |
| 阶段五：获准发布与持续评价 | [本机隔离工程演示设计](2026-09-20-prediction-model-refactor-phase-5-local-demo-release-design.md)与[五步计划](../plans/2026-09-20-prediction-model-refactor-phase-5-local-demo-release.md) | **【合成路线】S1–S4 已完成，实施4／5**：认证浏览器、停止受理与回退、可复算工程评价、会话内权限／幂等检查记录、发布记录闭合均已完成；真实数据库集成因缺少精确环境而跳过，尚未实际连接数据库、启动演示或发布；**【真实资料】尚未开始**，后续结局回流不自动触发重训或模型替换 |

**阶段一验收条件的实际状态（2026-09-20 补充）：** 上表阶段一条件中的「独立患者与候选窗口**分别**计数」**未满足**——实际只交付了候选计数，**独立患者数始终为「未建立／未知」**（阶段一记录载「所有 `patient_group_id` 目前为空」；阶段二计划载「独立患者数仍未建立」「当前获准标签／正式样本／分区登记数 0／0／0」）。其余条件（原文定位可追溯、来源与时间精度及未知结局保留、给出数据能支持与不能支持的任务）已满足。

该未满足项**不阻塞**合成路线（阶段二至四已按合成数据工程范围完成），但**阻塞真实资料路线的正式组样与分区**——它正是阶段二 四.\*、五.\* 各项的前置条件，已计入本表「后续真实资料正式冻结」的 23 条开放项。阶段一的准确状态因此是：**当前范围（两病种 Excel 清洗归一化）已交付；正式验收条件部分未满足，随真实资料阶段处理**，不得简写为「阶段一已完成」。

### 7.1 阶段一的首个实施包

阶段一已获准，仅针对已经提供的本地文件形成可复核清单：

1. 固定来源身份，检查段落、表格及内嵌图片中的数据承载情况，说明文本提取覆盖与未读部分。
2. 提取候选观测与时间关系，标注重复、家系、病因、检测情境及治疗上下文。
3. 逐例记录哪些结局已证实、哪些随访不足，以及需要回查原始记录的具体问题。
4. 汇总真实独立患者数、访视分布、关键字段覆盖、可用标签和不同窗口支持情况；受控明细与可提交的去标识汇总分开。
5. 提交下一阶段任务选择所需证据，以及 AD 公开临床表的具体补齐清单。此包不训练、不导入业务库、不修改活动模型。

现有自动初筛计数不能直接作为该实施包的最终验收结果。人工核验覆盖、尚未读出的图像信息和不确定标签必须明确。

### 7.2 每阶段统一完成记录

记录输入版本、实际改动、实际执行的验证、结果、失败或跳过、限制、产物位置和下一阶段条件。需要更新本总领文档时保留决策记录；不把待办写成完成，不将无模型可发布写成预测能力已完成。

代码验证遵循 [开发指南](../../DEVELOPMENT.md) 和 [报告运维文档](../../OPERATOR_REPORT_OPERATIONS.md)。涉及 UI 时先完整读取 [设计规范](../../DESIGN_SPEC.md)。数据库、worker、归档和权限行为使用适用的隔离集成验收，不能以 mock 结果代替真实链路。

**阶段出口门槛（2026-09-20 补充）：** 任何阶段在记为完成前，除本阶段定向回归外，还必须从 `backend` 运行整仓非集成回归（`python -m pytest tests ../scripts/tests --ignore=tests/integration --ignore=tests/e2e`）并**全绿**；出现任意新增失败即阻断结项，不得只凭定向回归宣布完成。该条源于阶段二至四均只跑定向回归，使 `database/schema.sql` 与 `ai_reports` 契约漂移潜伏了两个阶段，详见[跨阶段审查问题清单](../notes/2026-09-18-prediction-phases-1-4-cross-phase-review.md)。

### 7.3 分阶段计划导航

按用户要求采用“一阶段一份计划文档”。本文约束整体方向；各阶段计划明确具体输入、任务、文件、验证和完成记录。先完成当前阶段的计划与审核，实施后再依据产物细化下一阶段；不将五个阶段的未知条件写成一份固定执行清单。

| 阶段 | 计划文档或编写条件 | 当前状态 |
| --- | --- | --- |
| 阶段一：真实数据盘点 | [阶段一实施计划](../plans/2026-09-09-prediction-model-refactor-phase-1-data-audit.md) | 当前范围的 Excel 清洗交付已完成；**正式验收条件部分未满足**（独立患者数未建立，见第 7 节表下说明），正式训练准备缺口见[阶段一结果](../notes/2026-09-09-prediction-model-phase-1-data-audit.md) |
| 阶段二：任务与评估契约 | [阶段二实施计划](../plans/2026-09-09-prediction-model-refactor-phase-2-task-evaluation.md) | 合成工程范围已结项、剩0步；原23条真实资料开放项保留供以后替换核验 |
| 阶段三：离线基线与候选比较 | [阶段三合成离线比较规划与计划](../plans/2026-09-15-prediction-model-refactor-phase-3-offline-comparison.md)；真实资料以后按冻结条件替换并重新评价 | T1–T6完成6／6，剩0步；完整实验与解释、版本差异和后续替换交接已完成。AD 12个月RF数值历史为优先合成候选，其他任务结论与不确定性分别披露；无临床有效性声明 |
| 阶段四：契约与报告接入 | [阶段四设计](2026-09-16-prediction-model-refactor-phase-4-history-integration-design.md)与[阶段四六步计划](../plans/2026-09-16-prediction-model-refactor-phase-4-history-integration.md) | **已完成 S1–S6（实施6／6）**，含真实隔离端到端总验收；未切换活动配置或发布模型。原两项保留已于 2026-09-20 关闭（见上表同项说明） |
| 阶段五：获准发布与持续评价 | [本机隔离工程演示设计](2026-09-20-prediction-model-refactor-phase-5-local-demo-release-design.md)与[五步计划](../plans/2026-09-20-prediction-model-refactor-phase-5-local-demo-release.md) | S1–S4 已完成，实施4／5；当前下一步 S5，需用户单独授权隔离库与外部 LLM。真实数据库集成8项因缺少精确环境而跳过；计划不提前授权实际演示发布、外部调用、数据库写入、提交或推送 |

用户批准执行某阶段后，按照该阶段计划完成既定工作及验证；新增实质范围另行说明。阶段记录应区分“计划已写”“执行完成”“验证通过”和“允许进入下一阶段”。

**术语与编号约定（2026-09-20 补充）：** 本项目文档中的「任务N」指两套**内容不同**的清单，引用时必须带前缀，否则「任务三」有二义：

| 写法 | 含义 | 出处 |
| --- | --- | --- |
| **阶段一·任务一至七** | 阶段一数据盘点的 7 个工作项（来源固定、病例段清单、患者核验、观测与时间、结局盘点、公开资料与 AD 补数、一致性核验） | [阶段一计划](../plans/2026-09-09-prediction-model-refactor-phase-1-data-audit.md) §3 |
| **阶段二·任务一至六** | 阶段二任务与评估冻结的 6 项任务，含 **36 个执行项**（即「13 已满足／23 开放」那一套） | [阶段二计划](../plans/2026-09-09-prediction-model-refactor-phase-2-task-evaluation.md) §5 |

**约定：** 本总领及后续文档中出现**不带前缀**的「任务二／三／四／五／六」，且与「36 项」「23 条开放项」同现时，**一律指阶段二的任务**。阶段一那 7 项的完成状态以其与阶段二 36 项的**承接关系**为准（对照表见阶段一计划文末「未勾选项的承接关系」），不另行计数、不单独设待办。

**阶段二「暂不收口」的口径（2026-09-20 澄清）：** 阶段二计划中三处「用户已要求暂不收口／继续原六项任务」指的是**真实资料**范围（临床定义、身份签核、样本、划分与准入门槛，即任务一至六的临床部分），该范围**至今仍然开放**；其后按**合成数据工程范围**结项的是另一条并行路线。两者范围不同、结论并存，**互不矛盾**：合成工程结项不改变、也不提前关闭真实资料范围的「暂不收口」。阅读阶段二记录时按此区分。

## 8. 审核与后续决策

### 8.1 本次审核希望确认的原则

- [ ] 只做脂肪肝和 AD，以真实病例泛化为主要目标，保留现有报告基础设施。
- [ ] 先盘点真实观测与结局，再冻结任务及评价，最后训练与接入。
- [ ] AD 优先补齐公开临床随访数据；脂肪肝依据真实复查和事件支持选择任务，不强行维持一年进展目标。
- [ ] 小样本方法作为可比较候选；不把更复杂的算法、更多合成行数当作重构成功标准。
- [ ] 支持任务分别成熟、数据不足时不估计；继续保留完整性、权限、审计和历史兼容边界。

### 8.2 后续必须基于数据决定的事项

这些是有明确决策阶段的研究问题，不是留给实现者任意补齐的字段：

| 事项 | 决策阶段与依据 | 条件不足时的处理 |
| --- | --- | --- |
| 各病种首发人群、终点与时间窗 | 阶段二；真实观测、事件支持和用户使用场景 | 缩小研究范围或补数据，不替换成不等价标签 |
| 临床标签及变化阈值 | 阶段二；原始记录、适用标准和专业复核 | 保留待核实，不进入正式标签集 |
| 可用样本量和验证精度 | 阶段二；独立患者、事件数、预定模型复杂度及评价目标 | 不虚构样本数门槛或宣布可靠泛化 |
| AD 公共数据获取及本地适配范围 | 阶段一、二；实际文件、使用条件和字段可比性 | 先完成本地盘点，明确未取得部分 |
| 具体算法、校准及解释方法 | 阶段三；冻结方案下的比较结果 | 保留基线或拒绝接入，不追求算法名称 |
| 新契约结构（阶段四）与上线切换方式（阶段五） | 契约结构：候选输出、兼容和隔离验收；上线切换：目标环境与发布范围 | 保留当前版本身份，不原地改写旧制品 |

### 8.3 决策记录

2026-09-16阶段四S1：用户要求继续后完成四个新模型／结果契约与纯推理模块及三个测试文件，新旧联合回归168项通过；独立复审的证据自洽、实际函数导入绑定两项问题已修复并通过定向复核。旧A/B包runtime通过，914项范围外文件／删除状态不变。保留旧日期／OLS模块冷启动导入NumPy这一既有依赖，纯推理无训练或文件／网络访问。阶段四实施1／6，剩5步，下一步S2单一固定RF重建与回放；没有重建正式制品、操作数据库、接入报告或发布。

2026-09-16阶段四S2：完成四个新构建／CLI／测试文件，197项联合回归及独立复审通过；对200条原训练记录仅重建一个固定RF。新包`outputs/numeric-history-integration/2026-09-16-v1`（canonical SHA `a6816ed1a30d9a65ae089f67746e98473f0514732883d98a2843d3bc90db2464`）通过正式预检、构建与独立新进程只读复核；4800条投影差异0，1200条RF逐值回放差异0（503 available／697 abstain／0 error），其余三任务和全部末次值基线保持旧B行为。旧A/B runtime通过，921项范围外文件／删除状态不变。阶段四实施2／6，剩4步，下一步S3；未操作数据库、接入报告、切换配置或发布，临床状态仍为not_assessable。

2026-09-16阶段四S3：完成严格版本路由、v3完整上下文及受理／readiness接入，保留原授权、锁和幂等顺序；0031与ORM增加NULL安全v6/doc.v3完整发布及保存事实降级保护，旧v5约束不变。联合回归161项及2项子测试通过，独立复审Approved；实际C与三种封存合成输入的离线上下文检查通过，参考使用内存空目录，不代表真实DB/RAG。A/B/C runtime及923项范围外文件／删除状态保全通过。专用TEST_DATABASE_URL未配置，真实数据库测试已编写但未执行，留S6验收；没有迁移数据库、训练、调用LLM或启用模型。阶段四实施3／6，剩3步，下一步S4报告执行与历史核验。

2026-09-16阶段四S4：完成v3独立说明、worker、确定性正文、v6发布逻辑及保存事实历史分派；候选三态与末次值基线独立，错误内容／漂移拒绝发布，旧版本规则保留。联合回归300项通过，独立复审Approved；实际C与三种封存合成输入离线报告检查通过，参考和说明为受控替身，不能代替真实DB/RAG/LLM链路。A/B/C runtime及926项范围外文件保全通过。隔离数据库测试已编写但未执行；没有操作数据库、调用外部LLM、修改UI/PDF、启用模型或提交推送。阶段四实施4／6，剩2步，下一步S5页面与PDF，S6完成隔离总验收。

2026-09-16阶段四S5：完成v3生成类型、页面三态与独立基线、逐任务算法、打印契约及新renderer；后端56项、前端79项不同用例、契约30项和构建通过，独立复审Approved。4份软件夹具／2个上下文页面与4份共10页PDF检查通过，窄屏版本长串溢出已修复；5份旧PDF和旧manifest原字节不变。新renderer SHA `328b86684e9c123ee776934b93e453a20ae4c49e4dbeb9929bcb0f53406acb6e`（**2026-09-20 该 renderer 已作废**：因 `services/report_document_builder.py` 补入阶段投影规则披露句——它是 renderer 的 28 项源码依赖之一——已按既有规则重建为 `38f18749e2ceed29eb12273c66f2a056e701f01db01b3ada7a5582e433357558`，见[阶段四计划](../plans/2026-09-16-prediction-model-refactor-phase-4-history-integration.md)第 3.6 节。此处保留 S5 当时的值。），绝对路径见阶段四计划第3.6节；A/B/C runtime及929项范围外文件保全通过。专用数据库未配置，新增归档集成用例只编写／语法检查，未操作数据库、调用外部LLM、启用模型或提交推送。阶段四实施5／6，剩1步、下一步S6隔离端到端验收与阶段五交接。

2026-09-18阶段四S6：用户要求继续完成S6后，先核对接手状态——工作区停在浏览器失败诊断的RED检查点（3项测试失败、实现未写），集成测试尾部另有4行误粘贴断言，均已修正并转为GREEN。新增失败诊断（截图、页面body、page error类型、已脱敏URL；诊断自身失败只记阶段与异常类型）与条件等待（`wait_for_shell` 等待侧边栏导航按钮，替代依赖隐式30秒点击超时）。三次真实apply：v1退出1（前端在`/auth/me`后未挂载，当时无任何页面证据）、v2退出1（首版就绪条件误用病例列表region，报告完成后应用停在报告详情面板；改用导航按钮后以已完成报告零LLM定向复现确认）、v3退出0（357.44秒、`status=passed`），独立复审三项加固后v4复跑同样退出0（296.84秒），记录以v4为准。runner最终51项。5份报告完成、5次真实说明生成（持久审计各1次invocation_started／task_finished）、5份归档原件与下载字节SHA一致；`queued_v2_completed_with_c_selected=true`、取消报告7未调用worker／LLM、5份报告404／403／幂等重放通过、恢复B与不存在当前包两阶段保存事实与PDF原字节不变、`page_errors=[]`。专用库集成49项（`external_llm=false`）；前端168项／契约30项／构建通过。失败产物`2026-09-18-v1`／`v2`全部保留。整仓回归首次为5 failed（另3项为本文件测试受`test_pdf_generation.py`污染`sys.modules`中`playwright`所致，已改为测试内重建导入并在模拟污染下验证通过），修复后复跑为2 failed／2290 passed／59 skipped；剩余2项失败可追溯到阶段二／三提交（`database/schema.sql`未同步 ORM 的 `operator_cases.engineering_source` 列、cleanup specs允许清单未登记阶段一至四设计文档），非本步引入。**另保留一项**：v5跨0031 upgrade的既有行保全未单独演练。阶段四实施6／6；未修改活动配置、未启用新包、未提交或推送，临床有效性仍未验证。

2026-09-21阶段五S4：完成可复算工程评价、会话内固定检查与发布记录闭合。`build_release_metrics` 从事实字典重算 admission／jobs／timings／llm_audit／authorization／history／pdf／identity 八个闭合块，`collect_demo_facts` 只在本次 seeded 用户／病例范围内统计、范围外记录即失败关闭，阶段耗时取自 `phase_entered` 审计事件自身顺序，历史块只重算已保存摘要而不加载当前模型；发布记录正常路径写 `stopped`、任何主失败或清理／评价错误写 `failed`，最终写入失败返回非零且不重新开放；新增只读 `--inspect-release`，遗留 `starting`／`running` 只报 `unconfirmed_stop`。浏览器会话新增固定检查序列（同源请求、三个种子身份）：非所有者 404、错误角色 403、幂等重放与真实 queued 取消，期望不成立即失败且不宣告就绪。TDD 后阶段五定向与阶段四 runner 联合 **251 passed／0 failed**；隔离集成 8 项因缺精确 `TEST_DATABASE_URL` skipped；独立复审发现并修复 1 项 Critical（LLM 审计原按「每报告一对」配对，与真实报告为每个模型任务各写一条 `task_finished` 的事实不符，已改为按 task 配对并以真实产物事件流补回归）及 3 项 Important（`stopped` 增加完整性门禁、归档 renderer 身份校验、评价失败与会话失败分码）；`JobMetrics` 增补 `phase_timeout_phases`。另有事务内 SQL 可用性探针在既有隔离库执行并全部回滚，行数不变。阶段五实施4／5、下一步 S5；未启动服务／浏览器、未调用外部 LLM、未提交或推送，临床状态仍为 `not_assessable`。
2026-09-21阶段五S2：完成数据库返回事实门禁、单外层事务种子、原子 `release.json`、C 包子进程环境及 Windows Job／POSIX process-group 监督生命周期；API、前端、报告 worker、PDF worker 固定顺序启动并使用闭合安全事件，子进程原始输出不落盘，主错误与清理错误分离。TDD 后 S2 定向81项通过，联合阶段五 schema 与阶段四 runner 共164项通过；独立复审发现并修复 POSIX 子孙逃逸及 runtime 失败遗留无记录目录两项 Important，复核后无 Critical／Important。精确 `TEST_DATABASE_URL` 未配置，真实数据库集成3项 skipped；真实静态命令因未提交代码返回 `uncommitted_release_code` 且无输出。阶段五实施2／5、下一步S3；未实际连接／写入数据库、启动服务／浏览器、调用外部LLM、迁移、清库、提交或推送，临床状态仍为 `not_assessable`。

2026-09-20阶段五S1：完成严格发布记录、冻结身份／阶段四权威验收原字节绑定、Git 工作树门禁和默认静态 CLI；输入及父级逐项 `lstat` 拒绝 symlink，重复／缩写参数、错误隔离库、libpq 重定向、端口及既有输出失败关闭。TDD 三组92项及阶段四 runner 51项联合通过，独立复审 Critical／Important／Minor 均0。真实静态命令因当前未提交代码按设计返回 `uncommitted_release_code` 且未创建输出。阶段五实施1／5、下一步S2；未连接数据库、调用外部LLM、启动服务／浏览器、提交或推送，临床状态仍为 `not_assessable`。

2026-09-20阶段五规划：用户确认先使用合成数据，真实资料以后替换，并批准首版采用本机隔离工程演示。书面设计及五步实施计划已完成：默认 dry-run 零副作用，实际会话须双开关并固定 `surgery_rag_phase4_test`／0031、C 包、renderer 和阶段四权威验收身份；停止后关闭受理、等待任务收敛、撤销子进程环境并形成工程评价。当前实施0／5、下一步S1；未实现入口，未连接数据库、调用外部LLM、启动会话、提交或推送，临床状态仍为 `not_assessable`。

2026-09-16阶段四规划：阶段三提交`bc3cd39`并推送后，用户要求开始下一步。本轮已基于实际代码和封存证据写出阶段四设计及六步实施计划；建议仅AD 12月接入RF历史数值，固定首seed并继承明确旧包其余参数，采用独立模型／报告版本保护旧任务。旧A/B包只读runtime核验通过；没有据配置文件推断当前运行进程的活动包，没有执行训练、迁移、外部LLM、接入或发布。阶段四实施0／6、下一步S1；真实数据仍延期独立处理。

2026-09-15至16日阶段三补充：用户要求根据总领及关联任务规划阶段三，确认“先用合成数据做，之后有真实数据再替换”，并依次要求执行下一步、每次列出剩余步骤。T1–T6现已完成6／6，详见阶段三第7–12节。T5完整实验120／120次拟合成功且两次复算通过；T6完成逐任务正负结果、不确定性、覆盖／越界、版本差异和真实资料替换交接。本轮用户再次确认没有真实数据；下一步为阶段四合成候选契约与报告接入的详细设计，不因缺真实数据回退或阻塞合成路线。阶段四实施尚未开始，未切换模型或发布；以后真实资料独立审核、适配及重新评价。下表保留早期决策记录。

| 日期 | 状态 | 内容 |
| --- | --- | --- |
| 2026-09-09 | 已确认的需求背景 | 用户限定两个病种，优先真实病例泛化；确认四份病例文档来自医院病历或检查报告 |
| 2026-09-09 | 待审核 | 用户要求先编写总领文档，批准后按文档分步骤重构；本版本未开始实施 |
| 2026-09-09 | 已确认的组织方式 | 用户要求规格之后按阶段分别编写计划；已新增阶段一计划。本次仅编写文档，不将规划请求解释为全部实施获准 |
| 2026-09-09 | 阶段一获准及执行记录 | 用户明确“确认，开始阶段一”，并确认本人复核；机器盘点和私有复核材料已完成，身份、时间线与临床标签未签核，尚未整体验收。后续训练／接入／发布没有开始 |
| 2026-09-09 | 阶段一清洗交付与阶段二设计 | 用户将交付收敛为两个 Excel、排除图片，要求 AI 核对来源，随后本人补齐年龄／性别；该范围已完成。按指定信息“预测线路修改或重构第一阶段”提交为 `6619808` 并推送。用户要求随后设计第二阶段，现已有单独计划草案，未开始执行或训练 |
| 2026-09-10 | 阶段二获准并开始执行 | 用户要求开始已写好的阶段二计划；本次已重新读取并锁定两个最新 Excel，核对来源关联，继续整理两病种任务支持。任务定义、临床标签及评价门槛没有因此自动获准，未进入阶段三训练。具体计数与限制集中保存在阶段二计划第 7 节 |
| 2026-09-10 | 首版人群范围获准 | 用户回复“按照你的建议做”：AD 的 MMSE 任务包含已痴呆患者，需来源明确诊断且能可靠评分；ALT 首版聚焦来源支持的代谢相关脂肪肝，保留常见代谢共病，其他可能明显影响指标的病因单独处理。逐例适用性、时间窗及误差仍按证据核对，未授权训练 |
| 2026-09-10 | 后续仅使用现有文件 | 用户确认“只能使用现有文件”，不具备联系原资料提供者补核对的条件。继续核查已有文字、表格及审计引用；无法建立的患者关联、时间或临床依据保留为限制，不推算补齐，也不要求用户重新填写全表 |
| 2026-09-10 | 当前准入判断：暂缓模型实施 | 原始 DOCX 有界回查未发现本轮时间候选字面完全丢失；词法保真不能替代语义审核。两任务的临床签核、已核验患者组、窗口／输入及评价契约仍未建立，因此不进入阶段三。保留 `candidate`／`needs_decision`，不宣称整库没有随访或阶段二冻结已完成；详见阶段二计划第 7.9 节 |
| 2026-09-10 | 最终整合复审中止 | 复审子代理报告误将私有审计 JSON 输出到工具记录，已立即停止并告知用户。事件不在文档中复述私有内容；最终复审失败及执行隐私边界失误保留，不以其他完整性检查通过替代最终验收 |
| 2026-09-10 | 继续核对现有文件与任务定义 | 按用户要求，以主流程白名单汇总核对全部 169 个首发指标候选条目的 7,482 个原文定位，并分类先前的 243 个时间线索块；补齐任务卡 v0.3 及技术评价方案。原文保真及词法分类不代替完整语义判断；关系未证实的记录不进入正式标签，不再要求用户补表或联系来源提供者。详见阶段二计划第 7.10 节 |

## 9. 依据与相关文档

- 当前执行入口：[report_execution.py](../../../backend/app/workers/report_execution.py)。当前适用性入口：[operator_case_readiness.py](../../../backend/app/services/operator_case_readiness.py)。当前发布集合：[longitudinal_release_set.py](../../../backend/app/services/longitudinal_release_set.py)。
- 既有模型调用设计：[2026-09-03 设计说明](2026-09-03-ai-operator-model-invocation-design.md)。本文拟议的能力集合调整需要后续新版本设计，不能用历史设计或本文草案直接绕过当前完整性检查。
- [FLD1 数据发布说明](https://datadryad.org/dataset/doi:10.5061/dryad.7d7wm3809)：个体数据来源、年龄编码、均值填补与研究目标。
- [ADNI 数据说明](https://adni.loni.usc.edu/data-samples/adni-data/) 与 [临床评估说明](https://adni.loni.usc.edu/data-samples/adni-data/clinical-assessments/)：纵向临床资料、量表覆盖与数据获取条件。
- [TabPFN 原始论文](https://www.nature.com/articles/s41586-024-08328-6) 与 [MAML 原始论文](https://proceedings.mlr.press/v70/finn17a.html)：小样本候选方法的依据，不构成本项目的效果证明。
- [概率校准文档](https://scikit-learn.org/stable/modules/calibration.html)、[生存分析说明](https://scikit-survival.readthedocs.io/en/stable/user_guide/00-introduction.html)、[TRIPOD+AI](https://www.bmj.com/content/385/bmj-2023-078378)：评估与报告方法参考，不替代本项目实际验证。

外部资料已在本次会话核对，取得新数据或实施相关方法时应再次确认适用版本。仓库历史方法验证文档中的模拟数据指标不能作为本次真实模型的验收依据。
