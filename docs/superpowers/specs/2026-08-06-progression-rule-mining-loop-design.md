# 疾病进展规律挖掘 · 端到端最小闭环设计（修订版 v3）

> 日期：2026-08-06（v1）→ 2026-08-06（v2，按 Codex 首轮意见修订）→ 2026-08-06（v3，按 Codex 复审意见修订）
>
> 状态：设计修订完成，待复审
>
> 范围：独立 `research/` 子项目（端到端最小闭环，方法验证）

## 0. 修订记录

**v2**（按 Codex 首轮 9 条意见）：视界可配置 + label=unknown + not estimable；患者级 repeated grouped CV + OOF；复合规则 max_conditions=4 + ground truth 只进 evaluator；前向生成顺序；匹配对照 + run-in + 持续偏离 + 滞后消融 + 非因果措辞；OOF 规则验证 + 列联表 + CI；Monte Carlo 规模实验；大 N 成功标准 + 现实单元只要求输出；独立边界（config.py 自带参考范围）。

**v3**（按 Codex 复审 7 条意见），逐条对应：

| # | 复审意见 | v3 处理 |
| --- | --- | --- |
| 1 | 12 月视界与 24 月植入规律不一致 | 实验分族：24 月方法验证实验 vs 12 月规模退化实验；每视界有各自的 planted_rules 与 hazard 校准目标；不同视界恢复率不在同一曲线比较（§5.1/§5.3） |
| 2 | 前向生成残留"先命中规则、后生成条件"循环 | 新增外生潜在路径 Z ∈ {none, R1, R2, R1∩R2}，先采样 Z 再生成 S/指标/hazard；重叠路径互补合并；分阶段 S 阈值保证 AFP 先于事件（§5.4） |
| 3 | landmark 资格与正例标签逻辑冲突 | 重写标签语义（5 条精确判定）；默认单 landmark、多 landmark 一律患者聚类（§5.5） |
| 4 | GroupKFold 5×5 非真重复 CV | 自定义 repeated stratified group splitter（患者分组+结局分层+变种子）；事件不足自动降折；明确 OOF 聚合、AUC 中位数口径、患者聚类 Bootstrap（§6.3） |
| 5 | 规则 OOF 验证仍不独立 | 明确每折流程（训练折发现→冻结→验证折评估）；稳定选择频率=训练折选中比例；组合控制 top-M/阈值数/候选上限/剪枝；按唯一患者计数（§8.1） |
| 6 | lead-lag 缺可实现偏离阈值 | 定义偏离算法（σ_meas 版本化预设 + 持续 2 窗阈值，只在训练折拟合）；对照改风险集匹配（§7.1） |
| 7 | Monte Carlo 汇总与崩盘点定义 | 恢复率按重复频率 + CI 半宽目标（≤0.10，未达标自动增重复）；"可靠性边界"替代崩盘点（§8.2） |
| 8 | evaluator 类型化匹配 + 跨种子歧义 | 分类/计数/连续阈值类型化匹配；大 N 成功标准改 Monte Carlo 方法验收（≥90% 种子通过）；e2e 分确定性回归 + slow/acceptance（§8.3/§10/§11） |

## 1. 背景与目标

现有「AI 操作者」预测模块是**单时点模式匹配**：输入一组指标，对照参考标准 + 已录病例库，输出"匹配等级"（`analyze_indicators` / `compute_composite_probability`）。它回答的是"这组指标像哪类已录病例"。（此定位经 Codex 确认准确。）

新方向是**疾病进展预测与临床规律挖掘**：例如脂肪肝达到一定程度后进展为肝硬化/肝癌。目标产出是**可解释的、全新的临床时序规律**，例如：

> 男 >50 岁，若糖化血红蛋白连续两次上升 + 血小板较基线下降 >20%，则 6 个月后进展风险升高。

这类规律的发现依赖**真纵向随访数据**（同一患者多次复查、记录指标序列、直到进展/终点）——已与用户确认：现实数据即此形态，但**当前可收集规模有限：150-300 例、随访 24-36 个月**。

本文档描述**端到端最小闭环**：用**含植入规律的模拟数据**跑通"模拟 → 窗口特征 → 模型 → 时序归因 → 规则挖掘 → 规律报告"全链路，验证方法论本身能否可信地产出规律（把植入的规律挖回来），并定量回答"按现实数据量，规则可信度到几成、可靠性边界在哪"。

## 2. 方法论（对同事方案的修正）

同事方案（已确认采用的完整版，方案 A）：

> 时间轴切窗口（每 6 个月）→ 训练模型预测"未来 2 年是否进展为肝硬化/肝癌" → 用 SHAP 时间归因反向计算"进展前 6 个月谁先行"。

对方案的必要修正（已与用户、Codex 对齐）：

1. **"先行" ≠ SHAP 重要性。** SHAP 归因的是"模型靠什么特征预测"，不是"真实时间轴上谁先发生"。正确做法是两条证据线互相**印证**：
   - **lead-lag 时间对齐（主证据）**：进展患者对齐确诊零点，逐窗口回溯，直接测"每个指标首次偏离自身基线"的时间顺序，并配**匹配未进展对照**排除共性偏离。
   - **时间滞后 SHAP + 滞后消融（佐证）**：比较同一指标不同滞后项的预测贡献，验证"模型在用先动时点的证据"。
   - 两条线结论一致时，报告的措辞必须是**"观察到的时间顺序与模型预测贡献一致"**，明确不能解释为因果先后。
2. **"不看准确率"要有前提。** 模型无信号时归因解释的是噪声。流程上先确认信号（患者级 OOF AUC 门槛，见 §6.3），有信号才做归因、才挖规则。
3. **生存/时间维度的诚实处理。** 固定视界二分类只在视界内可估时成立；真实数据阶段主框架应是 landmark / discrete-time hazard 或时间相关生存模型（见 §12 后续子系统）。

## 3. 范围

### 3.1 做（最小闭环）

- 独立 `research/` Python 子项目，与现有 backend/frontend **完全解耦**。
- 模拟纵向数据生成器（前向潜在过程、含植入规律、删失、噪声、可复现种子族）。
- 窗口特征工程、进展模型（患者级 OOF 验证）、时序归因、规则挖掘。
- **独立评分器（evaluator）**：唯一允许接触植入规律 ground truth 的模块。
- **规模退化实验**（Monte Carlo）：不同 N / 随访 / 视界下量化规则恢复率与可靠性边界。
- Markdown 规律报告 + 端到端"植入 → 挖回"恢复测试（大 N 强信号 fixture）。

### 3.2 不做 / 边界（明确留出）

- **独立边界（Codex #9）**：`research/` 自带版本化配置 `config.py`（含指标参考范围常量），**禁止**运行时 import 生产 `prediction_engine.py`、禁止访问生产数据库、禁止写入现有表/API。
- 真实数据导入管线（模板解析 + 指标字典 + 长表入库）——真实数据未到位。
- Cox / 生存分析框架——最小闭环用固定视界二分类，生存框架留作后续。
- 规则部署到现有引擎（`progression_rules` 表 + 操作者端进展预测 UI）——后续子系统。
- 任何临床结论——模拟数据仅供方法验证。

## 4. 架构与目录

```text
research/
  requirements.txt       # pandas, scikit-learn, shap（matplotlib 可选）
  README.md              # 运行方式 + 参数说明
  main.py                # CLI 编排：simulate→features→model→attribution→rules→evaluate→report
  config.py              # 版本化配置：指标参考范围、模拟参数、实验网格（不自生产 DB）
  simulate_cohort.py     # 模拟纵向数据生成器（外生潜在路径 Z + 前向生成，输出数据集 + planted_rules）
  features.py            # 窗口切片 + landmark 化 + 指标派生特征
  splitters.py           # 患者级 repeated stratified group splitter + 患者聚类 Bootstrap
  model.py               # 进展二分类 + 患者级 OOF 验证
  attribution.py         # lead-lag（匹配对照 + run-in + 持续偏离）+ 时间滞后 SHAP/消融
  rules.py               # 规则挖掘（复合深度，阈值从数据/模型，禁植入语义）
  evaluator.py           # 独立评分器（唯一可接触 planted_rules 的模块）
  scale_study.py         # 规模退化 Monte Carlo 实验
  report.py              # Markdown 规律报告
  outputs/               # 运行产物（报告、模型、图）
  tests/
    conftest.py
    test_simulate_cohort.py
    test_features.py
    test_model.py
    test_attribution.py
    test_rules.py
    test_evaluator.py
    test_scale_study.py
    test_report.py
    test_end_to_end.py
```

每个模块单一职责、接口清晰、可独立单测。Codex 审查按独立子项目对待。

**关键数据流约束（Codex #3）：** `simulate_cohort.py` 返回「数据集 + 植入规律对象（planted_rules）」。数据集流向 `features → model → attribution → rules`；`planted_rules` **只流向 `evaluator.py`**。`rules.py` 的接口签名**不得接收** planted_rules——植入语义仅用于最终评分，绝不可进入挖掘器（防答案泄漏）。

## 5. 模拟数据生成（simulate_cohort.py）

### 5.1 配置与实验分族（CLI 可覆盖）

两组实验，**视界不同 → 植入规律与 hazard 校准目标各自独立（见 §5.3），不同视界的恢复率不在同一"规模退化"曲线上比较**：

- **方法验证实验（视界 24 个月）**：N=1500，随访 60 个月，6 个月/窗口 → 10 个窗口，视界 24 个月。目标：证明全栈 pipeline 在数据充足时能完整恢复植入规律。
- **规模退化实验（视界 12 个月）**：N ∈ {150, 300, 600, 1500}，随访 ∈ {24, 36, 60}，视界 12 个月（24 个月随访下 12 个月视界有可行 landmark；24 个月视界在短随访下不可行，见 §5.5）。
- 随机种子族固定 → 全流程可复现。

### 5.2 指标集（12 个）

肝进展相关连续指标：ALT / AST / GGT / TBIL / ALB / PLT / HbA1c / AFP / 腰围 / BMI。

静态协变量：年龄（岁）、性别（男/女）。

参考范围由 `research/config.py` 提供（自包含版本化常量），不依赖生产 `reference_ranges`。

### 5.3 植入规律（ground truth，只进 evaluator；按视界分族）

植入规律对象（planted_rules）按视界分别定义，含 规则条件、方向、时间窗、滞后语义、"触发 → 进展"延迟，供 `evaluator` 评分。**不同视界的规则不得混用**：

**视界 24 个月（方法验证实验）：**

- **规则 1（复合，4 条件）**：男 >50 岁 + HbA1c 连续 2 窗上升 + PLT 较基线下降 >20% → 24 个月内进展率约 **60%**。
- **规则 2（单指标）**：AFP 连续 ≥2 窗上升 → 24 个月内进展率约 **40%**。

**视界 12 个月（规模退化实验）：**

- **规则 1（12 月）**：同条件（男 >50 岁 + HbA1c 连续 2 窗上升 + PLT 较基线下降 >20%）→ 12 个月内进展率约 **40%**。
- **规则 2（12 月）**：AFP 连续 ≥2 窗上升 → 12 个月内进展率约 **25%**。

**hazard 校准目标（按视界，Z 为外生潜在路径，见 §5.4）：**

| 视界 | 基线（Z=none） | 规则 1 | 规则 2 | 规则 1∩规则 2（互补合并） |
| --- | --- | --- | --- | --- |
| 24 个月 | 12% | 60% | 40% | ~76% |
| 12 个月 | 6% | 40% | 25% | ~55% |

互补合并规则：`P(R1∩R2) = 1 − (1 − P(R1))·(1 − P(R2))`。模拟器须按 Z 分组自检进展率与目标一致（差异 >±3pp 视为模拟器缺陷，属模拟器验收，与规则挖掘无关）。

**植入的时序次序（lead-lag 的 ground truth，两个视界通用）：**

- 早期信号：HbA1c 逐窗上升、PLT 逐窗下降（命中规则 1 的患者），在距进展 **≥6 个月**即开始偏离自身基线。
- 临近信号：AFP 连续上升（规则 2）发生在**进展前 1-2 个窗口**（由 §5.4 分阶段阈值**前向保证**，非统计巧合）。
- 即植入的"先行次序" = **PLT / HbA1c 先行 → AFP 后行**。

### 5.4 生成顺序（前向，杜绝循环定义；Codex #4 + #2）

严格前向生成，**禁止**任何"先定事件时间、再反向构造预测特征"的逻辑。所有随机量前向依序采样：

1. **静态协变量**：年龄、性别。
2. **外生潜在路径 Z**（在生成任何指标之前采样）：`Z ∈ {none, R1, R2, R1∩R2}`，按目标患病率抽样，使 Z=none 群体的基线进展率匹配 §5.3 校准表。Z 是外生变量，**规则的命中由 Z 直接决定**，而不是事后从指标反推——这是消除"先命中规则、后生成条件"循环的关键。
3. **基线指标**（run-in 窗口的观测）：从分布抽取（部分落在脂肪肝异常区间）。
4. **潜在进展状态 S(t) 前向演化**：Z 决定 S 的动态——`Z=R1` → S 自某窗口起加速上升（驱动 HbA1c 逐窗上升、PLT 逐窗下降）；`Z=R2` → S 趋向高位（驱动 AFP 上升）；`Z=R1∩R2` → S 取两者较强者（风险按 §5.3 互补合并）；`Z=none` → S 缓慢/不升。
5. **分阶段 S 阈值（保证 AFP 时序 ground truth）**：定义两个版本化阈值 `S_AFP < S_event`，且 `S_event ≥ S_AFP + δ`（δ 对应 ≥1 窗的最小间隔）。当 `S(t) ≥ S_AFP` → AFP 开始上升；当 `S(t) ≥ S_event` → 事件 hazard 激活。由此"AFP 先于事件 1-2 窗"是**前向构造的确定语义**（近端标志物，非因果风险因素），不是同一潜在过程的统计巧合。
6. **逐窗口 hazard**：`λ(t) = f(年龄, 性别, Z, S(≤t))`；事件时间 = 首次触发的窗口。
7. **指标观测 X(t)**：`X(t) = g(S(t), Z) + 个体间噪声 + 测量噪声`。测量噪声 σ_meas 由 `config.py` 版本化预设（供 §7.1 偏离阈值使用）。
8. **删失时间 C**：独立于事件时间（~20% 随机失访）。
9. **截断观测**：`T = min(事件, 删失, 行政随访终点)`，其后窗口观测截断。

### 5.5 视界、landmark 与标签语义（Codex #1 + #3）

**Landmark 资格**（预测点 P 的判定，全部满足）：

1. P 时患者**尚未事件、尚未删失**；
2. 行政随访终点满足 `administrative_end − P ≥ 视界`（**按行政随访时间，不是按实际观察时间**）；
3. ≥2 个历史窗口（含 run-in）。

**标签规则**（P 之后，精确判定）：

- **正例**：视界内先发生事件；
- **负例**：无事件且实际观察满视界；
- **未知（label=unknown）**：视界内先删失且未发生事件——从标注集排除（不是"未进展"），记录排除比例；
- **not estimable 单元**：无可行 landmark（如 24 个月随访 × 24 个月视界）→ 只进报告，**不计入恢复率**。

**Landmark 数量策略**：

- 默认**每患者单 landmark**（最早满足资格的预测点）；
- 多 landmark 为后续增强——若启用，所有指标、支持数、CI 一律**按患者聚类**（一个患者的多个 landmark 不重复计数）。

以 6 个月窗口计：24 个月随访在 24 个月视界下**无可行 P**；36 个月至多 1 个；12 个月视界下 24/36 个月随访有可行 P。

## 6. 特征工程 + 模型（features.py / model.py）

### 6.1 窗口特征（landmark 化）

每个指标、每个窗口派生：

- 当前值
- 较 6 个月前差值（Δ6m）、较 12 个月前差值（Δ12m）
- 斜率（相邻窗口线性拟合）
- 连续上升次数
- 较基线下降百分比（基线 = run-in 窗口均值，见 §7.1）

静态特征：年龄、性别。所有特征仅使用 **P 及 P 之前**的历史窗口（无未来信息）。

### 6.2 标签与视界

- 视界可配置：方法验证单元 24 个月；现实单元 12 个月（§5.5）。
- 标签按 §5.5 语义（含 label=unknown 排除）。

### 6.3 模型与验证（患者级，Codex #2 + #4）

- sklearn `GradientBoostingClassifier`（免 xgboost 原生依赖，后续可替换）。
- **患者级 repeated stratified group splitter**（`splitters.py`，**非裸 GroupKFold**）：
  - 按**患者分组**（同一患者所有窗口样本只落在一个折）+ **按结局分层**（每折事件/非事件比例近似总体）+ **每重复不同随机种子**（各重复分折不同，构成真正的 repeated CV）；
  - **事件不足自动降折**：折数 = `min(5, 事件数, 总患者数)`，下限 2；事件数 < 2 时标 not-estimable。
- **OOF 聚合口径**（固定）：
  - 每重复内：该重复所有折的验证预测合并为该重复的联合 OOF 预测 → 每重复一个 OOF AUC；
  - 同一 landmark 在跨重复产生的多份 OOF 预测 → 取概率均值作为该样本最终 OOF 预测（用于规则验证的 OOF，§8.1）；
  - **"AUC 中位数" = 跨重复的每重复 OOF AUC 的中位数**（明确口径，不是跨折）。
- 评估指标：AUC（跨重复中位数 + **患者聚类 Bootstrap 95% CI**：B=1000、固定种子，按患者 resample）、PR-AUC、Brier score。**废弃单次 70/30 切分的单点 AUC**（现实规模下 30% 验证集仅约 5-11 个事件，单点不稳）。
- **信号门槛**（计算口径固定后启用）：仅方法验证单元（大 N）适用——患者级 OOF AUC 的 95% CI **下界 ≥ 0.65**；现实单元只报告不设门槛。不达标 → 归因与规则挖掘直接停止并亮红灯。

## 7. 时序归因（attribution.py）

### 7.1 lead-lag 时间对齐（主证据，描述性；Codex #5 + #6）

- **偏离阈值（可实现、确定算法）**：指标 t 窗口的偏离判定为 `|X(t) − run-in 均值| > κ·σ_meas + τ` **且持续 ≥2 个连续窗口**（changepoint，非单窗尖峰）。其中 σ_meas = `config.py` 版本化预设的**指标测量噪声**（模拟阶段由模拟器已知，真实数据阶段改用训练折残差 MAD）；κ、τ 为版本化常量。**阈值只在训练折拟合/读取，验证折只套用**。
- **基线**：run-in 窗口（前 2 窗）均值；个体内波动只用 run-in 窗口刻画（用全随访算 SD 会引入未来信息泄漏）。
- **对照（风险集匹配）**：以进展患者事件时间为 **index time**，为其匹配 1 名**在 index time 尚未事件且随访覆盖 ≥ index time 的未进展患者**，赋予相同伪零点；匹配变量 = 年龄（分箱）× 性别，允许替换（with replacement）；无合格对照 → 标记 unmatched 并报告比例（不用"精确或倾向匹配"的歧义表述）。
- **汇总**：每指标"首次偏离窗口中位数"，进展组 vs 对照组的差异 + **患者级 Bootstrap 95% CI**。
- **并列处理**：两指标中位窗口并列时，按进展组命中事件数破平。

### 7.2 时间滞后 SHAP + 滞后消融（佐证，描述性；Codex #5）

- 特征按（指标 × 滞后）展开，滞后 ∈ {当前窗, 前 1 窗, 前 2 窗}。
- 相关滞后特征之间会分摊 SHAP，`mean|SHAP|` 大小不能单独证明时间先后 → 增加**整组滞后消融/permutation**：把某指标的全部滞后作为一个组，比较保留/移除该组对模型 OOF 指标的改变，与保留当前窗的基线对照。
- 报告滞后的预测贡献分布（含 Bootstrap CI），**措辞限定为"模型预测贡献的时间滞后一致性（描述性）"**。

### 7.3 结论规则

- 两条证据线各自产出"时间顺序 + 预测贡献滞后"的排序；一致才写入报告，措辞为**"观察到的时间顺序与模型预测贡献一致"**，明确**不解释为因果先后**。
- 小 N（150-300 例）下 SHAP 不稳定，仅作佐证，不单独决定规则去留。

## 8. 规则挖掘 + 规模退化实验（rules.py / scale_study.py）

### 8.1 规则挖掘（Codex #3 / #6 / #5）

**每折独立流程（发现与验证严格分离）**——模型拥有 OOF 预测，不代表规则经过 OOF 验证：

1. 对每个训练折：
   - **候选特征 top-M**（如 top-8）：按该训练折内 SHAP 重要性选出；
   - **阈值选择**：从训练折分位数或 SHAP 引导的特征值选择；**禁止读取植入规律定义**（数据流约束见 §4，`planted_rules` 只进 `evaluator`）；
   - **候选生成**：单条件到复合规则，最大条件数 max_conditions=4；**组合控制**：每特征阈值数 ≤ T（如 ≤3）、beam-search/Apriori 宽度或最大候选数 ≤ C（如 ≤5000）、训练折支持度剪枝；
   - **发现规则**：按训练折 lift/support 排序选出规则 → **冻结**；
2. **验证折**（该折内从未参与该折的发现）：计算列联表 + lift——**规则先冻结、再在验证折算 lift，禁止先在全体数据发现规则、再在同一批患者的所谓 OOF 上评估**。

**汇总**：

- **稳定选择频率** = 规则在多少个训练折中被选中（跨折选中比例）；
- 每条候选规则的最终证据 = 各验证折列联表合并 + **患者聚类 Bootstrap 95% CI**；
- **支持约束（分开、按唯一患者计数）**：规则支持**事件数 ≥ 5**（唯一患者）且总支持数 ≥ 20（唯一患者）——一个患者多个 landmark 只计一次，5 个命中样本不能作为单一门槛。

年龄/性别作为普通候选条件参与，不做特殊预先分层。

### 8.2 规模退化实验（Monte Carlo；Codex #7）

- **网格**：**仅 12 个月视界族**（与 24 个月方法验证实验分开，恢复率不在同一曲线比较）：`N ∈ {150, 300, 600, 1500}` × `随访 ∈ {24, 36, 60}`；无可行 landmark 的单元标 not estimable，只进报告不计入恢复率。
- **每格重复 R = 50 次（最低值）**，种子族；**目标：恢复率的重复级 95% CI 半宽 ≤ 0.10**——未达标自动增加重复次数（每次 ×2，上限 R_max = 200）。
- 每次重复记录：名义 N、**可用患者数**（剔除视界不足）、**可用 landmark 数**；实际事件数、OOF 验证事件数；删失/视界不足导致的**排除比例**；完整恢复率、部分恢复率（见 §8.3）。
- **汇总（弃用单次 0/0.5/1 中位数）**：
  - 每条植入规则在 R 次中被**完整恢复的频率**；
  - **两条同时完整恢复的频率**；
  - 总体平均恢复率 + **重复级 95% CI**。
- **可靠性边界（替代"崩盘点"）**：固定视界下，随事件数下降，**最后一个"恢复率重复级 95% CI 下界 ≥ 50%"的事件数**；网格未跨越该阈值 → 输出 **"未观察到崩盘点"**（不报不存在的最小单元）。
- 输出"可信度 vs 例数"表，直接回答：按现实 150-300 例、随访 24-36 个月，规则可信到几成。

### 8.3 命中语义（evaluator 定义；Codex #3 / #8）

**类型化匹配**（不同条件类型用不同容差，禁用统一的 ±10% 相对容差）：

- **分类条件**（性别）：精确匹配；
- **次数/窗口条件**（连续上升次数、时间窗）：整数容差 ±1；
- **连续阈值**：绝对或相对容差——阈值接近 0 时用绝对容差，否则相对 ±10%；
- **horizon、lookback、lag**：**分别比较**，不得混淆。

**命中定义**：

- **完整命中**：指标集、方向、时间窗、horizon/lookback/lag 均与植入规则一致，数值阈值在类型化容差内。
- **部分命中**：指标集 + 方向一致，但时间窗或数值阈值未达容差（或条件子集命中）。
- 恢复率 = 完整命中数 / 植入规则总数（该视界族 2 条）；部分恢复率单独报告。

## 9. 规律报告（report.py）

Markdown，固定结构：

1. **摘要**：方法、数据规模、植入规律 vs 挖回规律总览。
2. **信号验证**：患者级 OOF AUC（中位数 + CI）、PR-AUC、Brier、信号门槛通过与否。
3. **挖回规则列表**：每条含 条件 / 方向 / 时间窗 / lift（含 CI）/ 支持事件数 / 总支持数 / 稳定选择频率。
4. **植入规则对照表**：ground truth vs 挖回结果，完整/部分命中标注（容差规则见 §8.3）。
5. **证据时间线**：进展组 vs 匹配对照的首次偏离次序（含 CI）。
6. **时间滞后 SHAP + 消融摘要**：top 滞后特征（描述性措辞）。
7. **规模退化表**：每（N × 随访）单元（12 月视界族）的重复数、排除比例、每条规则完整恢复频率、两条同时恢复频率、总体平均恢复率 + 重复级 CI、可靠性边界；not estimable 单元单独标注。
8. **局限与下一步**：模拟数据非临床结论、现实事件数约束、后续子系统清单。

## 10. 成功标准（最小闭环"通过" = 全部满足）

1. **方法验证：Monte Carlo 方法验收测试（视界 24、N=1500、60 月、K=20 个种子）**：
   - 每个种子的"通过"判定 = **三条同时成立**：① 患者级 OOF AUC 95% CI 下界 ≥ 0.65；② 两条植入规则均完整命中（§8.3 类型化容差）；③ lead-lag 次序恢复（中位首次偏离窗口次序与植入一致，容差 ±1 窗，并列按事件数破平）。
   - **验收通过 = ≥90%（18/20）种子通过**——不是要求每个种子通过，也不是单种子。
   - 跨种子通过率本身是结果指标，写入报告。
2. **现实单元（150/300 例、24/36 月、视界 12）**：报告 Monte Carlo 重复统计（每条规则完整恢复频率、两条同时频率、总体平均恢复率 + 重复级 CI、排除比例、**可靠性边界**）——**只要求输出与退化结论，不要求恢复**。
3. **可复现**：固定种子族，同一命令重跑得到同一报告。

## 11. 依赖与测试

### 依赖

- `pandas`、`scikit-learn`、`shap`；`matplotlib` 可选（仅时间线图）。

### 测试（延续项目 TDD 纪律，分三层）

- **快速单测（普通 pytest 运行）**：模拟器产出结构/植入信号/前向性、特征计算正确性（无未来泄漏）、患者级分组 splitter 与 OOF 指标、lead-lag 排序、SHAP/消融比较、规则每折流程与 lift/support、evaluator 类型化命中语义、报告完整性。
- **确定性端到端回归（标记 `slow`，不随普通单测运行）**：大 N 强信号 fixture（N=1500、60 月、视界 24、单一固定种子）断言两条植入规则被挖回 + lead-lag 次序恢复——**仅作回归，不验证跨种子通过率**。
- **Monte Carlo 方法验收（标记 `acceptance`，仅显式运行）**：§10 的 K=20 种子跨种子通过率 ≥90% 的验收测试——**单种子 e2e 不能替代它**。
- **现实规模测试**：断言 pipeline 正常产出、输出结构完整、退化指标被量化（不要求恢复）。
- 运行：`research` 目录内 `python -m pytest tests/`（默认跑快速层；`-m slow` / `-m acceptance` 分别跑长流程层）。

## 12. 现实约束与后续

### 现实约束（写入报告）

- 现实 150-300 例 × ~12% 进展率 → **进展事件仅约 18-36 个**，SHAP 处于不稳定区。
- 随访 24-36 个月 → 窗口仅 4-6 个；短随访下**视界只能取 12 个月**（24 个月视界在 24 个月随访单元下不可估，标 not estimable）。
- 结论仅对模拟数据成立；真实数据到位后需重跑并独立验证（真实数据阶段主框架为 landmark / discrete-time hazard 或时间相关生存模型）。

### 后续子系统（Phase 2+，另行设计与实施）

1. **真实数据导入管线**：Word 固定模板 → 严格校验解析 → 指标名归一化字典 → 长表入库 → 导入 API。
2. **生存框架**：landmark / discrete-time hazard 或时间相关协变量 Cox，替代固定视界二分类。
3. **规则部署**：`progression_rules` 表 + 操作者端"进展预测"UI，生产端套用挖出的规则（延续现有"模式匹配参考"措辞约定）。
