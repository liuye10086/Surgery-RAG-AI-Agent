# 手动验收缺陷修复计划

目标：修复 MANUAL_TEST_RESULTS_2026-09-08.md 中范围明确的问题，保留原始验收结论并追加复验记录。

方案：沿用现有 schema、来源授权、标准版本锁及请求错误提示。不升级依赖、不执行业务库迁移，不修改两份报告或归档原件。真实数据库验证只使用用户已指定的 surgery_rag_test。

- [x] 注册：先增加空白用户名、短密码和合法边界测试；RegisterRequest 对用户名去首尾空白后要求非空，密码沿用前端至少 6 位要求，登录兼容现有账号。
- [x] 来源权限：先验证 ai_operator 对 both 全文/图片读取，以及普通用户仍须自己的引用；补齐两处授权分支；以真实 PG API 矩阵复验。
- [x] 候选状态：复现 materialized → accepted → 重复实体化；审核使用与实体化一致的“版本锁 → 刷新候选并加锁”顺序，禁止修改已实体化候选和已批准/退役版本；验证并发、持久规则和审计数量。
- [x] 分块错误：以损坏 PDF 验证响应、持久 error_message 均不包含原始异常；统一安全提示；页面删除重复弹窗，保留列表刷新和 loading 复位；运行前端行为测试和构建。
- [x] 测试夹具：复现 registry 临时模型元数据与当前训练环境不匹配、子进程连接串密码被掩码、相对路径依赖 cwd；只修改对应测试夹具，保留生产兼容性拒绝断言。
- [x] K02：用户已要求补齐标题编辑；前后端、真实标题检索及回归已完成。
- [x] 验收：相关 pytest 红绿验证后运行后端非集成回归、真实 PG 集成、前端单元/契约/构建；检查改动及两份 PDF 哈希，追加结果与限制。不提交或推送。
