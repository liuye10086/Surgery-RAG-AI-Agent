# 统一数值报告第3步：总验收与版本切换

> 使用 subagent-driven-development 执行，重型检查串行。范围来自已批准的统一方向，不另开字母包。

**Goal:** 提供可重复的隔离验收入口，实际验证两病种完整流程、数据与模型切换、历史及 PDF 原件不变，并收口当前三步工程路线。

**Architecture:** 复用现有版本化导入、固定模型包、真实 HTTP/API、worker CLI、浏览器及 PDF 归档。新生成一个固定种子的合成数据版本并实际训练第二模型包；不手改系数伪装训练。以两个已验证制品运行切换，受理前后版本及旧历史分别核对。

## Global Constraints

- 仅显式本机 `_test` 数据库；进程级配置，不修改 `.env`、业务库或发布指针，不提交推送部署。
- 后端保留 synthetic 来源；真实来源契约如用虚构 fixture 验证必须在验收记录明示，不能作为真实临床数据。
- 原训练包、历史制品和 PDF 不覆盖；旧报告不得调用当前模型、检索、LLM 或 renderer 补算。
- 复用当前实现，不改已冻结模型实现、报告模板和数据契约；发现实际问题先复现再最小修复。
- Python 3.11.4、Node.js 22.15.0、npm 10.9.2；训练、测试、BGE、浏览器串行，无模型下载。
- 仅本轮合成输入及合成参考可调用已配置 DeepSeek；失败如实保留，不自动重试或模拟成功。
- 原阶段二第5节36项逐字保留；工程通过不关闭真实数据事实、定义和评价条件。

## 任务一：持久验收入口

Files: 新增 `scripts/run_numeric_report_acceptance.py`、`scripts/tests/test_run_numeric_report_acceptance.py`；必要场景辅助文件限定验收范围。

- [x] 先验证参数、隔离库、已存在输出、端口占用及外部调用授权门控；失败不能触碰数据。
- [x] 复用上一轮实际脚本与 `run_operator_report_e2e.py` 的进程管理；CLI 显式提供 source/model A/B、renderer、全新 output、`--apply`、`--allow-external-llm`；默认只读预检。
- [x] 明确记录两病种的旧/新数据及模型身份、幂等请求、排队固定版本、API 重启、历史和 PDF 原件字节一致性；取消、越权与关闭门控拒绝。
- [x] 必须保存实际结果 JSON、日志及页面/PDF证据；失败退出非零，拥有的进程树在正常与异常退出清理，禁止接管占用端口服务。

## 任务二：执行统一验收

- [x] 冻结本轮前文件摘要；用新种子20260915生成两病种120人/挑战40人的新合成版本，计算基线并实际训练第二Ridge包，核对与A参数不同。
- [x] 在私有 PostgreSQL 实例迁移、受控参考索引、构建当前renderer，实际执行持久验收入口。
- [x] 串行运行新入口测试及适用导入、v2/v5、旧版本兼容、租约/失败/PDF恢复集成；记录 fake adapters 与真实外部执行的区别。
- [x] 检查浏览器和新PDF；保全旧输出、阶段二原清单，停止任务进程和私有数据库。

## 任务三：收口文档

- [x] 独立审查本次验收入口和结果，修复重要问题并验证。
- [x] 更新总领文档、阶段二计划开头及追加记录、README 和运行说明；区分工程路线完成与正式阶段二待条件事项。
- [x] 交付修改、实际检查、限制；明确下一步与剩余步骤。

完成：见[本轮验收记录](../notes/2026-09-15-numeric-unified-acceptance-result.md)。当前工程路线已全部完成，正式真实资料条件保留。
