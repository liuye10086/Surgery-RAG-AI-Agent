# 全项目功能与手动测试清单

核对基线：`main` / `c9d3e24`，2026-09-08。根据当前页面、已注册 API 和后台任务整理；这是待执行清单，不代表已完成手动验收。

启动前先阅读 [启动前检查与本机现状](STARTUP_CHECKLIST.md)。执行建议：账户 → 管理员准备资料 → 问答 → 病例 → 报告 → 历史 → PDF → 权限与异常 → 删除清理。

每项通过后勾选；失败记录编号、账号角色、操作、预期、实际、截图/错误代码。没有前置资料的项目记为“未测/阻塞”，不要记为通过。删除、停用、退役、断网和故障注入只针对专门的测试资料及测试环境。

## 1. 入口、角色和测试资料

| 身份 | 页面入口 | 主要能力 |
| --- | --- | --- |
| 未登录 | `/login` | 注册、登录 |
| 普通 `user` | `/`、`/settings` | 知识问答、个人数据导出、删除自己的账户 |
| 管理员 `admin` | 上述入口及 `/admin`、`/operator` | 文档、疾病、标准管理及自己的操作者病例/报告 |
| AI 操作者 `ai_operator` | `/operator`；可直接访问 `/settings` | 自己的病例、预测报告、历史、PDF；不能使用聊天或管理员接口 |

当前没有医生/患者模式切换或网页账户角色管理入口；旧角色显示文案不代表这些完整流程已实现。管理员可进入操作者工作台，但病例、报告仍按用户归属隔离。

准备资料：管理员、普通用户、操作者 A/B（两个独立浏览器配置或一个无痕窗口）；两病种虚构病例；普通文字 PDF、扫描 PDF、带表格/图片 DOCX、图片、WPS 异常关系 DOCX；三个访问范围的知识文档；专用标准 DOCX 和可退役的测试版本；用于第二页验收的至少 21 个测试病例。

四份原始病例仍位于 `C:\Users\86182\Desktop\项目相关文件-2026-7-9`，文件名见启动指南。它们是生成/原始资料验收输入，不会因为放在该目录就自动进入聊天知识库或参考病例索引。

## 2. 账户和导航

- [ ] A01 注册：用户名、邮箱、密码、可选姓名；成功后回登录页，普通注册固定为 `user`。
- [ ] A02 输入校验：空值、错误邮箱、短密码、重复用户名/邮箱有可理解错误，不产生重复账户。
- [ ] A03 正确/错误密码登录；重复点击期间有加载状态；失败不留下有效登录态。
- [ ] A04 登录后刷新、直接输入受保护地址、退出后刷新；身份与跳转正确。
- [ ] A05 普通用户不能访问管理/操作者页；操作者访问聊天/管理页被拦截；管理员三类入口可用。
- [ ] A06 过期/无效 token 返回登录页；前端入口隐藏之外，直接 API 同样拒绝越权。
- [ ] A07 侧栏展开/收起、头像/角色标签、退出、返回按钮、键盘焦点和窄窗口布局正常。
- [ ] A08 更换账号后，聊天、病例、报告、PDF、危险提示不残留上一账号内容。

## 3. 知识问答与会话

- [ ] B01 新会话空状态和推荐问题可用；发送第一问后会话入列表，自动标题更新并可恢复。
- [ ] B02 输入框发送按钮和 Enter、空白输入、超长输入校验正常；一轮生成期间不重复发送。
- [ ] B03 有知识库命中的问题：显示检索/生成状态，回答逐步输出，完成后刷新仍可读。
- [ ] B04 多轮追问包含指代、省略、缩写时，保留上下文，引用与当前问题相符。
- [ ] B05 切换科室/全部科室，检索范围正确；停用科室不在可选列表。
- [ ] B06 无匹配资料时出现无知识提示，不伪装成有来源的回答。
- [ ] B07 多会话切换、读取历史及删除会话；删除确认可取消，其他会话不受影响。
- [ ] B08 正在回答时新建/切换会话，中止当前观察；旧 token、标题、完成或错误回调不能写进新会话。
- [ ] B09 回到被中止会话，状态可理解；失败占位可重新生成，界面不永久 loading。当前没有单独“停止”按钮，使用切换/新建触发该路径。
- [ ] B10 模拟发送响应丢失后重试：只保留一条用户问题，沿用原幂等身份，不把临时负数 ID 发作服务端回答 ID。
- [ ] B11 服务端已记录失败回答后“重新生成”，刷新验证回答持久化，后续可正常追问。
- [ ] B12 重试旧问题后，最新问题的危险状态不被旧问题覆盖。

## 4. 引用来源、图片和内容安全

- [ ] C01 点击正文引用编号，高亮对应来源；引用卡片展开/收起正常，标题/页码对应资料。
- [ ] C02 信息来源侧栏按问题分组；按钮及 Ctrl+B 开关正常，空态正确。
- [ ] C03 “查看完整病例”可打开对应内容，分块顺序、表格信息和病例归属正确。
- [ ] C04 来源中有图片时可看缩略图、放大、切图；受认证图片正常加载。
- [ ] C05 `chat`/`operator`/`both` 文档权限同时覆盖检索、全文和图片；未授权或删除资料有明确错误。
- [ ] C06 输入危险症状测试文本，出现匹配的问题级警示；刷新/切会话后不错误串用。
- [ ] C07 越狱/注入测试文本被规则拦截；输出安全检查及免责声明显示正常。
- [ ] C08 Markdown、表格、链接正常显示；测试 HTML/脚本内容不会在浏览器执行。

## 5. 管理员：知识库文档

- [ ] D01 选择/取消/重新选择文件，填写标题、科室、访问范围并上传；列表显示类型、大小、状态和上传时间。
- [ ] D02 文字 PDF 上传→分块→预览→向量化；状态与分块数量正确。
- [ ] D03 扫描 PDF 和 JPG/JPEG/PNG 经真实 OCR 后能预览文字并检索，不能把空内容当成功。
- [ ] D04 DOCX 段落/表格交替时保持原顺序；病例 1 的表格不会归到病例 2。
- [ ] D05 含内嵌图片的 WPS 异常关系 DOCX，修复后文字和图片均保留；原始文件不被改写。
- [ ] D06 不支持/损坏文件、超限文件、伪装后缀返回错误；`.doc` 虽可选择，解析时应提示另存为 `.docx`，不作为支持的成功路径。
- [ ] D07 标题/文件名搜索、清空搜索、科室筛选正常；无结果有空态。
- [ ] D08 分块预览抽屉：页码、编号、字符数、正文及删除单个块；后续检索不再返回已删除块。
- [ ] D09 重新向量化后只返回当前有效代次，不混入失效/重复内容；大文档超时提示后可刷新核对实际状态。
- [ ] D10 删除测试文档的确认/取消；成功后列表、分块、向量检索与图片访问状态一致。

文档列表支持“编辑标题”：验证保存、取消、刷新后保留、500字限制、保存失败重试，以及清空后回退文件名。标题更新后，聊天全文检索应使用新标题。科室/权限编辑和科室管理有后端接口，但当前管理页面没有完整编辑表单，见第 12 节。

## 6. 管理员：疾病管理

- [ ] E01 查看名称、稳定代码、启停状态和病例/参考病例/报告/标准引用数。
- [ ] E02 新建无引用的测试疾病；空名称、非法/重复代码被拒绝，代码创建后不能修改。
- [ ] E03 编辑显示名称和描述；既有病例仍使用同一疾病身份。
- [ ] E04 停用/启用疾病，操作者目录与写权限随之变化。
- [ ] E05 停用后历史病例、报告、已归档 PDF 仍可读取；病例写入、生成及删除受到限制。
- [ ] E06 无引用测试疾病可确认删除；有引用疾病仅可停用，直接 API 也拒绝删除。
- [ ] E07 新建任意疾病字典不等于自动获得模型和指标目录；未配置疾病不能伪装为可预测。

## 7. 管理员：参考标准

- [ ] F01 标准文档库上传 DOCX，标题、哈希、大小、时间正确；错误格式或重复内容有明确反馈。
- [ ] F02 未关联源文档可以删除；已关联源文档锁定且不能删除。
- [ ] F03 按疾病创建标准集合；同一疾病重复建集合被阻止；全部已创建时按钮说明正确。
- [ ] F04 选择可用源文档并填写版本标签，新建 draft；关联后源文档状态同步。
- [ ] F05 draft 解析生成源片段与规则；查看规则类型、可计算/evidence-only 状态、解释及来源。
- [ ] F06 可编辑规则修改上限并填写原因；空原因不可保存，锁定状态不可编辑。
- [ ] F07 提交审核进入 review；只有符合状态的操作可用，发布问题数和可投影规则数正确。
- [ ] F08 审核通过发布 approved；当前标准切换，新报告使用正确版本且来源可追溯。
- [ ] F09 发布前缺来源、规则错误、绑定不匹配必须阻止；evidence-only 内容不能变成数值阈值结论。
- [ ] F10 批准版本退役；旧报告维持保存内容，不能用新规则改写历史。
- [ ] F11 删除 draft/review 的确认和取消；已批准版本不可直接删除；文档关联状态正确释放。
- [ ] F12 快速切标准/版本、解析/发布期间重复点击，不出现旧版本数据覆盖或对错版本操作。
- [ ] F13 候选规则审核/实体化和变更历史为 API 验收项；须执行第 12 节 K03，覆盖 P1 修复。

## 8. 操作者：病例与访视

- [ ] G01 新建病例填写疾病、年龄、性别、基线阶段、备注；系统生成唯一匿名编号。
- [ ] G02 脂肪肝和 AD 的阶段/指标目录分别正确；已建病例疾病锁定，不跨病种误用指标。
- [ ] G03 年龄、性别、阶段及各字段错误定位到输入；中文错误不暴露内部堆栈。
- [ ] G04 1～10 次访视添加/删除，至少 1 次才能保存；保存后日期排序和访视编号连续。
- [ ] G05 空/重复日期、超出访视上限、空指标、重复/未知/跨疾病指标、不合法单位有错误提示。
- [ ] G06 指标增删、数值和单位选择；旧指标/旧单位显示历史提示并要求处理，不静默替换。
- [ ] G07 检测信息：来源、机构、设备、平台、方法、样本、是否基线、治疗/诊断变化、备注保存后恢复；留空不补造信息。
- [ ] G08 AD 特有量表版本、语言、受教育年限/校正，脂肪肝影像类型正确显示；不合疾病的上下文被拒绝。
- [ ] G09 首次保存、失败重试/重复提交只建一个病例；实际修改必须有原因；无变化保存不更新时间或增审计。
- [ ] G10 已有/新建草稿切到历史或报告再返回仍保留，保存失败也保留；刷新页面不承诺恢复草稿。
- [ ] G11 草稿切到其他病例/新建时确认丢弃；取消继续编辑；确认期间编辑/保存/删除不冲突，过期确认不影响新会话。
- [ ] G12 病例搜索、活动/归档筛选、总数、上一页/下一页；至少 21 例可到第二页，筛选从首页开始。
- [ ] G13 删除末页最后一例自动回有效页；快速筛选/翻页的迟到响应不覆盖当前列表，翻页不清除正在编辑的病例。
- [ ] G14 仅修改/新增/删除访视，保存后病例更新时间和最近修改排序正确；失败回滚不改变原时间。
- [ ] G15 归档/恢复有原因和确认，归档病例只读；未保存、正在保存或生成时操作限制正确。
- [ ] G16 删除病例需确认；成功后回到新病例/有效列表，原有历史报告和 PDF 仍保留，另一用户病例不受影响。

## 9. 报告准入、生成与取消

- [ ] H01 未保存草稿、资料缺失、少于 3 次访视、归档/停用疾病不能生成；显示具体阻塞原因。
- [ ] H02 脂肪肝肝硬化前期、肝硬化，AD MCI 的至少三访视病例分别生成报告，阶段与选用模型正确。
- [ ] H03 疑似肝硬化、已到目标结局或模型不支持的阶段显示限制，不编造不适用的风险分数。
- [ ] H04 提交后看到排队、阶段进度和唯一报告 ID；HTTP 受理成功不等于正文已生成。
- [ ] H05 连点/重发同幂等键得到同一报告；同键不同内容、同病例已有活动任务或队列已满有明确反馈。
- [ ] H06 刷新、返回病例、断线后重新打开报告，恢复数据库中的状态，不另建一份任务。
- [ ] H07 用户显式取消，状态最终为已取消或已完成之一；仅离开页面不能当成取消。
- [ ] H08 暂停 worker 后任务排队，恢复 worker 可处理；关闭受理时不接新任务，历史仍可读。
- [ ] H09 worker 中途退出/执行超时由独立 sweep 收敛为失败；重启不能自动重算已失效 running；有明确错误阶段。
- [ ] H10 SSE 断线、读取失败、限流 Retry-After、取消与完成竞争有稳定显示；不能从不确定响应推断成功。

## 10. 完整报告阅读、证据与模型

- [ ] I01 完成报告显示匿名编号、日期、输入满足状态、选中/调用/可用模型数量、信号数和证据状态。
- [ ] I02 11 节目录及锚点可用：摘要、病例/范围、质量/适用性、观察变化、365 天风险、阶段/趋势可用性、信号、参考证据、局限性、人工复核、技术附录。
- [ ] I03 已观察趋势图与录入访视数值/日期/单位一致；不把观察折线当未来预测。
- [ ] I04 365 天结局、阶段、下一访视趋势分开解释；模型未校准明确标注，不能把模型分数称为已校准真实概率。
- [ ] I05 模型缺项、指标缺失/不可比、检测条件变化进入数据质量/局限性，不输出伪完整结论。
- [ ] I06 正式标准标明疾病、版本、来源段落、适用条件和单位；可计算规则与 evidence-only 分开。
- [ ] I07 有合格参考病例时显示匿名身份、可比性、来源、可靠性、结局和数据版本；无病例/索引过期/查询失败有对应降级说明。
- [ ] I08 保存输入快照、生成审计、模型/数据版本可追溯；修改原病例/更新标准后，旧报告内容不随之改变。
- [ ] I09 失败/取消报告只展示已保存资料与已确认审计，不展示为完整完成报告；旧报告缺完整快照显示限制。
- [ ] I10 隔离测试中的报告完整性损坏被识别，阻止正文/图表/PDF；不能靠重新推理掩盖历史损坏。

## 11. 历史、PDF、个人数据

- [ ] J01 历史按病种、匿名编号、状态、日期范围筛选及清除；起始晚于结束有错误，日期按上海时区处理。
- [ ] J02 侧栏与历史页加载更多，返回后筛选/滚动可恢复；状态更新提示、读取重试正确，不重复漏项。
- [ ] J03 历史摘要显示访视数、模型版本、PDF 状态和下载次数；点开读取保存报告。
- [ ] J04 完成报告准备 PDF：未准备→排队→准备中→原件已归档；离开页面后再进入仍能恢复。
- [ ] J05 PDF 中文、上标单位、表格、分页、页眉页脚、图表和正文完整，无乱码/截断。
- [ ] J06 重复下载同一原件，内容哈希一致，下载次数按成功交付增加；下载不调用模型或重新渲染。
- [ ] J07 从未发布的 PDF 准备失败可显式重试；已发布原件缺失/损坏只能恢复同一原件，不重新生成替代品。
- [ ] J08 删除报告确认/取消；生成中禁止删；删除后不可读/下载，202 提示文件清理中，cleanup 最终完成。
- [ ] J09 普通/中文用户名导出 JSON，账户、会话、消息、审计内容属于本人；HTTP 响应头合法。操作者报告完整导出范围见 API 返回，不能按页面描述推断。
- [ ] J10 测试账户删除：错误密码拒绝，正确密码后登出；账户/关联资料处理与审计匿名化正常，知识库保留，PDF 文件清理由 outbox 补偿。

## 12. 无完整页面入口的 API 功能

后端启动后在 `/docs` 登录授权，或用浏览器 Network/接口客户端操作。以下全部路径以 `/api/v1` 开头；使用专用测试数据。界面未提供按钮的功能不要误记为网页已实现。

- [ ] K01 科室管理：`/admin/departments` 的 GET/POST，以及 `/{id}` PUT/DELETE；名称/描述、启停、重复名称、文档引用保护。公共 `/departments` 的 active_only 筛选。
- [ ] K02 文档详情/元数据更新：`/admin/documents/{id}` GET/PUT，修改标题、科室、访问范围后验证权限与聊天检索同步。
- [ ] K03 标准候选：`/admin/reference-standard-versions/{id}/candidates` GET；`/admin/reference-standard-candidates/{id}` PATCH；`/{id}/materialize?reason=...` POST；验证合法实体化、未确认、重复、无权限、缺原因等分支。
- [ ] K04 标准版本 validation/history、源片段、规则列表和规则 PATCH；合法状态/原因与来源绑定约束正确。
- [ ] K05 操作者 `/diseases`、`/diseases/{code}/indicators`、`/reference-ranges`、`/documents` 查询与许可/访问范围匹配。
- [ ] K06 文档全文 `/documents/{id}/content`，受认证图片 `/files/images/{doc_id}/{generation}/{filename}` 及旧图片路径；越权和旧代次不能泄露数据。
- [ ] K07 `/auth/login` 表单、`/auth/login/json` JSON、`/auth/me`；`/user/export` 与 DELETE `/user/account`；归属和错误状态正确。
- [ ] K08 报告新任务 `/operator/longitudinal-cases/{id}/report-jobs`；状态、events、cancel、PDF prepare/retry/download 的认证、UUID 幂等头与限流。
- [ ] K09 旧 `/operator/longitudinal-cases/{id}/reports` SSE 兼容入口：兼容开关开时按现实现受理，关闭时返回 410，不另走同步模型链。
- [ ] K10 `/admin/dashboard` 仅为受权限保护的简单接口，不是完整管理统计页面；`/health` 仅证明 Web 已就绪，不代表所有报告服务就绪。

## 13. 运维与离线工具

这些是项目能力，但不需要每次页面回归都重新训练或导入。只读检查可先做；导入、激活、修复、恢复、删除等写操作按各工具参数审阅后在测试环境执行。

- [ ] L01 数据库版本/扩展、正式标准来源与运行配置检查；识别启动指南所列旧检查器误报。
- [ ] L02 四份原始 DOCX 验收、两病种纵向数据构建、验证、导出；输出可复现、来源区分明确。
- [ ] L03 数据集显式 release/hash 导入和激活、参考病例窗口 dry-run/apply；无活动版本时不能直接构建索引。
- [ ] L04 模型训练/评估/审查/启用/回滚/停用及 registry smoke；当前模型/数据制品加载正确，历史版本可追溯。
- [ ] L05 RAG 基线评估 `scripts/evaluate_rag.py`；向量/全文某分支故障时能降级，外层事务不被错误破坏（后者用集成测试）。
- [ ] L06 回答提交失败不能发送 done；可选审计失败不能撤销已保存回答（隔离数据库/故障注入）。
- [ ] L07 报告/PDF 独立 sweep、文件 cleanup，在 worker 停止时仍处理过期或已登记清理任务。
- [ ] L08 PDF 原件 inspect、同哈希恢复、备份清单、删除事实导出和恢复重放；恢复不能复活已删除报告。
- [ ] L09 前后端测试、真实 PG 集成、浏览器 E2E 和固定 renderer 验收；仅对明确 `_test` 数据库运行种子/破坏性测试。

## 14. 当前占位/不应作为已完成功能验收

| 项目 | 当前情况 |
| --- | --- |
| 独立图片管理页 | 开发中占位；文档入口图片 OCR、病例图片阅读可测试 |
| 视频/手术视频检索 | 开发中占位 |
| 知识分类页 | 禁用/占位；疾病字典和科室接口另外验收 |
| 网页账户管理、改密、找回密码、角色分配 | 当前没有这些完整界面/接口流程；管理员/操作者由脚本创建 |
| 医生/患者双模式切换 | 当前没有可操作切换入口 |
| Agent 模式 | 配置预留，不能作为已实现自主工具调用流程 |
| 旧自由文本预测/重复病例工作区 | 已清理，验收统一纵向病例工作区 |
| 对任意新增疾病自动预测 | 不支持自动补齐指标、模型和标准 |
| 刷新浏览器恢复未保存病例草稿 | 当前只保证工作台内切历史/报告的保留 |

## 15. 三访视虚构测试样例

仅供软件测试，不是正常参考范围或诊断样例。三个日期可用 2026-01-01、2026-02-01、2026-03-01；在同一测试病例逐次添加。阶段正常成功路径可先用脂肪肝“肝硬化前期”和 AD“轻度认知障碍”。

| 病种/指标 | 单位 | 第一次 | 第二次 | 第三次 |
| --- | --- | --- | --- | --- |
| 脂肪肝 ALT | U/L | 42 | 44 | 46 |
| AST | U/L | 32 | 34 | 36 |
| GGT | U/L | 48 | 50 | 52 |
| TBIL | μmol/L | 16 | 17 | 18 |
| ALB | g/L | 42 | 41 | 40 |
| PLT | 10⁹/L | 200 | 190 | 180 |
| AFP | ng/mL | 4 | 5 | 6 |
| AD MMSE | 分 | 27 | 26 | 25 |
| MoCA | 分 | 25 | 24 | 23 |
| CDR | 分 | 0.5 | 0.5 | 1 |
| NfL / plasma_nfl | pg/mL | 16 | 18 | 20 |
| p-tau217 / plasma_ptau217 | pg/mL | 0.2 | 0.25 | 0.3 |

年龄、性别、检测条件按虚构测试设定填写，不填写真实身份。先测 1 次访视保存及报告被阻止，再补至 3 次测生成；不要求每项模型一定给出分数，应按报告准入和适用性说明判断。

## 16. 问题记录模板

| 用例编号 | 账号角色 | 测试资料标识 | 操作 | 预期 | 实际/错误代码 | 结果与截图 |
| --- | --- | --- | --- | --- | --- | --- |
| 例如 G14 | ai_operator | 自建匿名病例 | 仅改第三次 ALT 并保存 | 时间刷新、列表靠前 | 待填写 | 待测 |

代码核对入口：`frontend/src/router/index.ts`、`views/`、`components/`、`stores/`，以及 `backend/app/main.py`、`api/`、`workers/` 和 `scripts/`。更改功能后同步更新清单。
